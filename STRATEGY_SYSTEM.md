# Стратегическая система генерации сообщений

**Дата:** 2024-11-14  
**Версия:** 2.0  
**Статус:** ✅ Реализовано

---

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Типы стратегий](#типы-стратегий)
3. [Матрица стратегий](#матрица-стратегий)
4. [Актуальный контекст](#актуальный-контекст)
5. [Процесс генерации сообщений](#процесс-генерации-сообщений)
6. [API и использование](#api-и-использование)
7. [Best Practices](#best-practices)

---

## Обзор системы

Стратегическая система генерации сообщений - это усовершенствованный подход к созданию персонализированных рассылок, который учитывает:

- **Стратегический тип** сообщения (не просто selling/useful/reminder)
- **Матрицу стратегий** по этапам воронки и теплоте лида
- **Актуальный контекст** (акции, кейсы, новости)
- **Историю коммуникации** с лидом
- **Примеры лучших практик** для AI

### Ключевые улучшения

✅ **5 стратегических типов** вместо 3 базовых  
✅ **Матрица стратегий** для умного выбора типа сообщения  
✅ **Актуальный контекст** для своевременных акций и кейсов  
✅ **Улучшенный промпт** с примерами и четкими инструкциями  
✅ **UI для управления** контекстами через фронтенд

---

## Типы стратегий

### 1. **CHECK-IN** (Проверка актуальности)

**Цель:** Мягко напомнить о себе, узнать актуальность темы  
**Когда использовать:**  
- Холодные лиды, которые давно не отвечали
- После пропущенной встречи
- Для лидов на этапе "thinking"

**Пример:**
```
Иван, добрый день! Решил ненадолго напомнить о себе. Ранее обсуждали с вами запуск рекламы для клиники. Подскажите, тема всё ещё актуальна или пока поставили на паузу? Если будет удобно вернуться к этому позже — просто дайте знать.
```

---

### 2. **VALUE** (Полезный контент)

**Цель:** Дать ценность, укрепить доверие, напомнить о себе через пользу  
**Когда использовать:**  
- Для прогрева холодных лидов
- После 2+ продающих сообщений подряд
- Для этапа "thinking" (помочь с решением)

**Пример:**
```
Иван, добрый день! Кстати, по стоматологиям сейчас хорошо срабатывает один простой приём в рекламе: акцент на конкретной услуге + понятный результат для пациента. Если будет интересно, могу скинуть короткий чек-лист из 5 пунктов, по которым мы оцениваем готовность клиники к рекламе.
```

---

### 3. **CASE** (Кейс/Результат)

**Цель:** Показать социальное доказательство, конкретные результаты  
**Когда использовать:**  
- Для warm/hot лидов
- При возражениях по цене
- Для неквалифицированных лидов (показать возможности)

**Пример:**
```
Иван, добрый день! Вспомнил нашу переписку и хочу коротко поделиться свежим кейсом. С похожей клиникой запустили рекламу имплантации: за месяц привели 150 обращений, из них 47 дошли до консультации. Если интересно, могу рассказать на примере, какие связки там сработали.
```

---

### 4. **OFFER** (Акция/Предложение)

**Цель:** Использовать ограниченное предложение как повод для действия  
**Когда использовать:**  
- ТОЛЬКО при наличии активного промо-контекста
- Для warm/hot лидов
- Как повод для возвращения неактивных лидов

**Пример:**
```
Иван, добрый день! Со следующей недели мы запускаем ограниченную акцию: настройка рекламной кампании для 3 клиник на специальных условиях (без фиксированной стоимости за запуск, только % от результата). Думаю, ваш формат хорошо под неё подходит. Если хотите, могу отправить детали.
```

---

### 5. **DIRECT_SELLING** (Прямая продажа)

**Цель:** Получить согласие на следующий шаг (звонок, запись, старт)  
**Когда использовать:**  
- Для hot лидов
- После successful прогрева (value → case → direct)
- Для квалифицированных лидов

**Пример:**
```
Иван, добрый день! С учётом того, что вы уже смотрите в сторону рекламы для клиники, предлагаю сделать простой шаг — короткий созвон на 15 минут. Покажу, как мы обычно выстраиваем воронку для стоматологий и что можно протестировать у вас в первую очередь. Когда вам удобнее — сегодня во второй половине дня или завтра утром?
```

---

## Матрица стратегий

Матрица определяет приоритет стратегий для каждой комбинации `funnel_stage` × `interest_level`.

### Пример матрицы

| Этап воронки | HOT | WARM | COLD |
|--------------|-----|------|------|
| **new_lead** | direct_selling, case, check_in | case, direct_selling, value | value, case, check_in |
| **first_contact** | direct_selling, case, offer | case, direct_selling, value | value, check_in, case |
| **thinking** | case, value, direct_selling | value, case, check_in | value, check_in |
| **price_objection** | case, value, offer | case, value, offer | value, check_in |
| **no_show** | check_in, direct_selling, case | check_in, direct_selling, case | check_in, value |
| **inactive** | value, case, offer | value, case, offer | value, check_in, offer |
| **not_qualified** | value, check_in | value, check_in | value, check_in |

### Логика выбора

1. **Базовый выбор:** Первая стратегия из списка для данного `stage` + `interest`
2. **Избежание повторов:** Если стратегия использовалась 2+ раза подряд → следующая в списке
3. **Boost от контекста:** Если есть активная акция/кейс → приоритет `offer`/`case`

---

## Актуальный контекст

### Типы контекстов

#### 1. **PROMO** (Промо-акция)
- Скидки, спецпредложения, ограниченные слоты
- Boost стратегию → `offer`

#### 2. **CASE** (Кейс)
- Свежие результаты, успешные проекты
- Boost стратегию → `case`

#### 3. **CONTENT** (Полезный контент)
- Чек-листы, гайды, статьи
- Boost стратегию → `value`

#### 4. **NEWS** (Новость)
- Обновления, новые услуги, события
- Boost стратегию → `value`

### Структура контекста

```typescript
{
  id: string;
  type: 'promo' | 'case' | 'content' | 'news';
  title: string;                        // "Скидка 20% до конца месяца"
  content: string;                      // Подробное описание
  goal?: string;                        // "Побудить к записи"
  start_date: string;                   // Когда начинает действовать
  end_date?: string;                    // Когда заканчивается (null = без срока)
  target_funnel_stages?: string[];      // [] = для всех этапов
  target_interest_levels?: string[];    // [] = для всех уровней
  priority: number;                     // 1-5, где 5 = высший
  is_active: boolean;
}
```

### Фильтрация контекстов

Контекст применяется к лиду, если:
- ✅ `is_active = true`
- ✅ Текущая дата в диапазоне `[start_date, end_date]`
- ✅ `target_funnel_stages` пусто ИЛИ содержит `lead.funnel_stage`
- ✅ `target_interest_levels` пусто ИЛИ содержит `lead.interest_level`

Из подходящих контекстов выбирается **топ 1 по priority**.

---

## Процесс генерации сообщений

### Шаг 1: Формирование очереди (Cron, 09:00)

1. Получить пользователей с `autopilot_enabled = true`
2. Для каждого пользователя:
   - Рассчитать `reactivationScore` для всех лидов
   - Отфильтровать заблокированных (deal_closed, key_stages)
   - Взять топ N по `reactivationScore` (N = daily_message_limit)

### Шаг 2: Генерация сообщений

Для каждого лида в очереди:

1. **Получить активные контексты:**
   ```typescript
   const contexts = await getActiveContexts(
     userAccountId,
     lead.funnel_stage,
     lead.interest_level
   );
   ```

2. **Определить стратегию:**
   ```typescript
   const { strategyType, messageType, selectedContext } = 
     await determineStrategyType({
       lead: { id, funnel_stage, interest_level, ... },
       lastMessageTypes: [...], // последние 3 strategy_type
       activeContexts: contexts
     }, userAccountId);
   ```

3. **Сформировать промпт:**
   ```typescript
   const prompt = buildMessagePrompt({
     lead,
     strategyType,
     messageType,
     businessContext,     // из business_profile
     activeContexts,
     templates,           // подходящие шаблоны
     daysSinceLastCampaign
   });
   ```

4. **Вызвать AI (GPT-4o-mini):**
   - Модель: `gpt-4o-mini`
   - Temperature: `0.8` (креативность)
   - Response format: JSON
   - Результат: `{ message, reasoning }`

5. **Сохранить в БД:**
   ```sql
   INSERT INTO campaign_messages (
     user_account_id,
     lead_id,
     message_text,
     message_type,      -- selling/useful/reminder
     strategy_type,     -- check_in/value/case/offer/direct_selling
     context_id,        -- ID использованного контекста (если есть)
     goal_description,
     status,            -- 'pending'
     scheduled_at
   )
   ```

### Шаг 3: Отправка (Worker, каждые 5 минут)

1. Проверить рабочие часы
2. Рассчитать слот (сколько сообщений можно отправить за 5 минут)
3. Взять `pending` сообщения
4. Отправить через WhatsApp API с задержкой 1-3 сек между сообщениями
5. Обновить статус → `sent` или `failed`

---

## API и использование

### Backend API (crm-backend)

#### Получить контексты
```http
GET /contexts?user_account_id=uuid
```

#### Получить активные контексты
```http
GET /contexts/active?user_account_id=uuid&funnel_stage=thinking&interest_level=warm
```

#### Создать контекст
```http
POST /contexts
Content-Type: application/json

{
  "user_account_id": "uuid",
  "type": "promo",
  "title": "Скидка 20%",
  "content": "Скидка 20% на первичную консультацию до 1 июня",
  "goal": "Побудить к записи",
  "start_date": "2024-05-01",
  "end_date": "2024-06-01",
  "target_interest_levels": ["hot", "warm"],
  "priority": 5,
  "is_active": true
}
```

#### Обновить контекст
```http
PUT /contexts/:id
Content-Type: application/json

{
  "is_active": false
}
```

#### Удалить контекст
```http
DELETE /contexts/:id?user_account_id=uuid
```

### Frontend (crm-frontend)

**UI для управления контекстами:**
- Страница: `ReactivationCampaigns` → вкладка "Актуальный контекст"
- Компонент: `CampaignContextsManager`
- Функционал:
  - Просмотр активных и неактивных контекстов
  - Создание/редактирование/удаление контекстов
  - Фильтрация по этапам воронки и теплоте
  - Приоритизация контекстов

**Обновлённые компоненты:**
- `TemplateManager`: добавлено поле `strategy_type` (опционально)
- `DailyCampaignQueue`: отображение `strategy_type` в виде badge

---

## Best Practices

### 1. Создание контекстов

✅ **DO:**
- Создавайте контексты для реальных акций и событий
- Указывайте чёткие даты начала/окончания
- Используйте приоритеты: 5 для срочных, 1-2 для фоновых
- Фильтруйте по целевой аудитории (hot/warm/cold)

❌ **DON'T:**
- Не создавайте вымышленные акции
- Не оставляйте истёкшие контексты активными
- Не создавайте слишком много контекстов одновременно (AI использует только топ-1)

### 2. Матрица стратегий

✅ **DO:**
- Используйте дефолтную матрицу как базу
- Кастомизируйте только для специфичных ниш
- Тестируйте изменения на небольшой выборке

❌ **DON'T:**
- Не ставьте `offer` первым для cold лидов
- Не используйте только `direct_selling` для всех этапов
- Не меняйте матрицу без анализа результатов

### 3. Шаблоны

✅ **DO:**
- Создавайте шаблоны для каждого strategy_type
- Используйте реальные кейсы с цифрами
- Обновляйте шаблоны на основе feedback

❌ **DON'T:**
- Не копируйте один шаблон для всех типов
- Не используйте устаревшую информацию
- Не делайте шаблоны слишком длинными (50-150 слов)

### 4. Мониторинг

📊 **Отслеживайте:**
- Response rate по strategy_type
- Conversion rate по context_id
- Эффективность каждого шаблона
- Частоту использования каждой стратегии

🔄 **Итерируйте:**
- A/B тестируйте разные подходы
- Анализируйте, какие стратегии работают лучше для разных этапов
- Обновляйте примеры в промпте на основе реальных успешных сообщений

---

## Техническая архитектура

### База данных

**Новые таблицы:**
- `campaign_contexts` - актуальные контексты
- `campaign_strategy_overrides` - кастомные матрицы стратегий

**Обновлённые таблицы:**
- `campaign_messages` +  `strategy_type`, `context_id`, `goal_description`
- `campaign_templates` + `strategy_type` (опционально)

### Chatbot Service

**Новые модули:**
- `strategyTypes.ts` - типы TypeScript
- `defaultStrategyMatrix.ts` - дефолтная матрица
- `strategySelector.ts` - логика выбора стратегии
- `contextManager.ts` - работа с контекстами
- `messageExamples.ts` - примеры сообщений
- `promptBuilder.ts` - построение промпта

**Обновлённые модули:**
- `messageGenerator.ts` - интеграция новой системы
- `campaignCron.ts` - сохранение strategy_type

### CRM Backend

**Новые routes:**
- `campaignContexts.ts` - CRUD для контекстов

### CRM Frontend

**Новые компоненты:**
- `CampaignContextsManager.tsx` - UI управления контекстами
- `campaignContextsApi.ts` - API клиент

**Обновлённые компоненты:**
- `ReactivationCampaigns.tsx` - новая вкладка "Актуальный контекст"
- `TemplateManager.tsx` - поле strategy_type
- `DailyCampaignQueue.tsx` - отображение strategy_type

---

## Roadmap / Будущие улучшения

### Краткосрочно (Q1 2025)
- [ ] A/B тестирование матриц стратегий
- [ ] Дашборд аналитики по стратегиям
- [ ] Автоматическое определение лучших примеров из истории

### Среднесрочно (Q2 2025)
- [ ] Machine Learning для предсказания лучшей стратегии
- [ ] Персонализация матрицы на основе конверсий
- [ ] Sentiment analysis для адаптации стратегии

### Долгосрочно (Q3+ 2025)
- [ ] Мультиязычная поддержка
- [ ] Интеграция с другими каналами (email, Telegram)
- [ ] Автоматическая генерация контекстов из новостей

---

**Версия документа:** 2.0  
**Последнее обновление:** 2024-11-14  
**Авторы:** Development Team

