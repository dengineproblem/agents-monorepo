# Исправление распределения бюджета в Auto-Launch V2 (LLM)

**Дата:** 20 ноября 2025  
**Проблема:** LLM не распределяла весь бюджет направления при создании нескольких ad sets  

---

## 🐛 Обнаруженные проблемы

### Проблема 1: Неполное использование бюджета

**Что было:**
- LLM создавала несколько ad sets, но выделяла каждому только по $10
- Пример: Бюджет направления $50, создано 3 ad sets по $10 каждый = $30 использовано, $20 остались неиспользованными

**Пример из логов:**
```
Direction: AI-трагитолог
Budget: $50 (5000 центов)
Created: 3 ad sets
Result: 3 × $10 = $30 использовано
Problem: $20 осталось неиспользованными ❌
```

### Проблема 2: Ad sets создавались выключенными

**Что было:**
- В коде `auto_activate` принудительно устанавливался в `false`
- Ad sets создавались в статусе PAUSED
- Пользователю нужно было вручную их включать

**Код (строка 249):**
```typescript
action.params.auto_activate = (request.body as any)?.auto_activate || false;
// ❌ По умолчанию false - ad sets выключены
```

---

## ✅ Внесенные исправления

### Исправление 1: Полное распределение бюджета в промпте

**Файл:** `services/agent-service/src/lib/campaignBuilder.ts`

#### Изменение А: Добавлено критическое напоминание в начало промпта

```typescript
🔥 КРИТИЧЕСКИ ВАЖНО - РАСПРЕДЕЛЕНИЕ БЮДЖЕТА:
При создании нескольких adsets ты ОБЯЗАН распределить ВЕСЬ бюджет направления 
(direction_info.daily_budget_cents) между всеми adsets.
Сумма daily_budget_cents всех adsets ДОЛЖНА РАВНЯТЬСЯ полному бюджету направления!

Пример: Если бюджет направления $50 (5000 центов) и ты создаешь 3 adsets:
✅ ПРАВИЛЬНО: 1700 + 1700 + 1600 = 5000 центов ($50)
❌ НЕПРАВИЛЬНО: 1000 + 1000 + 1000 = 3000 центов ($30) - осталось $20 неиспользованными!
```

#### Изменение Б: Обновлена логика формирования adsets

**Было:**
```
КОЛИЧЕСТВО ADSETS ОПРЕДЕЛЯЕТСЯ БЮДЖЕТОМ (фиксированно $10 на adset):
- $10-19 → 1 adset по $10-19
- $20-29 → 2 adset по $10-14.5
- $50-59 → 5 adset по $10-11.8
```

**Стало:**
```
ВАЖНО: ВСЕГДА РАСПРЕДЕЛЯЙ ВЕСЬ БЮДЖЕТ НАПРАВЛЕНИЯ ПОЛНОСТЬЮ!

КОЛИЧЕСТВО ADSETS И РАСПРЕДЕЛЕНИЕ БЮДЖЕТА:
- $10-19 → 1 adset с ПОЛНЫМ бюджетом ($10-19)
- $20-29 → 2 adsets, ВЕСЬ бюджет делим поровну ($10-14.5 каждому)
- $50-59 → 5 adsets, ВЕСЬ бюджет делим поровну (~$10-11.8 каждому)

ФОРМУЛА БЮДЖЕТА НА ADSET:
budget_per_adset = ОКРУГЛИТЬ_ВВЕРХ(total_budget / number_of_adsets)
```

#### Изменение В: Обновлены примеры распределения

**Добавлен наглядный пример для $50:**

```
$50 (3 adsets), 6 креативов:
  Adset 1: [креатив1, креатив2] — $17 (1700 центов)
  Adset 2: [креатив3, креатив4] — $17 (1700 центов)
  Adset 3: [креатив5, креатив6] — $16 (1600 центов)
  ✅ ВЕСЬ бюджет $50 использован ($17 + $17 + $16 = $50)
```

#### Изменение Г: Обновлены правила

**Правило 4 (критически важное):**
```
🔥 КРИТИЧЕСКИ ВАЖНО: Распределяй ВЕСЬ бюджет направления полностью между всеми adsets!
   - Сумма daily_budget_cents ВСЕХ adsets ДОЛЖНА РАВНЯТЬСЯ полному бюджету direction_info.daily_budget_cents
   - Формула: budget_per_adset = ОКРУГЛИТЬ(total_budget / number_of_adsets)
   - Пример: Бюджет $50, 3 adsets → $17 + $17 + $16 = $50 ✅
```

#### Изменение Д: Обновлены примеры решений

**Добавлены конкретные примеры с расчетами:**

```
Пример 4: Бюджет $50, 6 креативов (3 low, 3 medium)
→ Создать 3 adsets по 2 креатива:
   - Adset 1: $17 (1700 центов)
   - Adset 2: $17 (1700 центов)
   - Adset 3: $16 (1600 центов)
   - ИТОГО: $50 (5000 центов) ✅
```

#### Изменение Е: Обновлен параметр auto_activate

**Было:**
```
- auto_activate = false (создаем в PAUSED для проверки)
```

**Стало:**
```
- auto_activate = true (создаем включенными для немедленного запуска)
- 🔥 ВАЖНО: daily_budget_cents в каждом adset должен быть рассчитан так, 
            чтобы сумма ВСЕХ adsets = полному бюджету направления!
```

#### Изменение Ж: Обновлены примеры форматов ответа

**Добавлен Пример Б для $50:**

```json
ПРИМЕР Б: Бюджет $50 → 3 adsets (распределяем весь бюджет!)
{
  "type": "Direction.CreateMultipleAdSets",
  "params": {
    "direction_id": "uuid-направления",
    "adsets": [
      {
        "user_creative_ids": ["uuid-1", "uuid-2"],
        "daily_budget_cents": 1700,
        "adset_name": "Set 1 - Top performers"
      },
      {
        "user_creative_ids": ["uuid-3", "uuid-4"],
        "daily_budget_cents": 1700,
        "adset_name": "Set 2 - Medium performers"
      },
      {
        "user_creative_ids": ["uuid-5", "uuid-6"],
        "daily_budget_cents": 1600,
        "adset_name": "Set 3 - New creatives"
      }
    ],
    "auto_activate": true
  },
  "reasoning": "Бюджет $50 → 3 adsets. ВЕСЬ бюджет распределен: $17+$17+$16=$50 (5000 центов)."
}
```

---

### Исправление 2: Auto-activate по умолчанию true

**Файл:** `services/agent-service/src/routes/campaignBuilder.ts`

**Строка 249 - Было:**
```typescript
action.params.auto_activate = (request.body as any)?.auto_activate || false;
// ❌ По умолчанию false - создаем выключенными
```

**Строка 249 - Стало:**
```typescript
// По умолчанию включаем adsets (auto_activate = true), если не указано явно false
action.params.auto_activate = (request.body as any)?.auto_activate !== false;
// ✅ По умолчанию true - создаем включенными
```

**Логика:**
- Если в запросе `auto_activate` не передан → используется `true` (создаем включенными)
- Если в запросе `auto_activate: true` → используется `true`
- Если в запросе `auto_activate: false` → используется `false` (только если явно указано)

---

## 📊 Ожидаемый результат

### До исправлений:

```json
{
  "direction_name": "AI-трагитолог",
  "budget": 5000,
  "adsets_created": 3,
  "budget_distribution": [1000, 1000, 1000],
  "total_used": 3000,
  "status": "PAUSED"
}
```

❌ Проблемы:
- Использовано только $30 из $50 ($20 остались неиспользованными)
- Ad sets выключены

### После исправлений:

```json
{
  "direction_name": "AI-трагитолог",
  "budget": 5000,
  "adsets_created": 3,
  "budget_distribution": [1700, 1700, 1600],
  "total_used": 5000,
  "status": "ACTIVE"
}
```

✅ Исправлено:
- Использован весь бюджет $50 (1700 + 1700 + 1600 = 5000)
- Ad sets включены (ACTIVE)

---

## 🧪 Тестирование

### Сценарий 1: Направление с бюджетом $50 и 6 креативами

**Команда:**
```bash
curl -X POST http://localhost:8082/api/campaign-builder/auto-launch-v2 \
  -H "Content-Type: application/json" \
  -d '{
    "user_account_id": "uuid-пользователя"
  }'
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "results": [
    {
      "direction_name": "AI-трагитолог",
      "mode": "llm",
      "action": "Direction.CreateMultipleAdSets",
      "adsets": [
        {
          "adset_name": "Set 1 - Top performers",
          "daily_budget_cents": 1700,
          "creatives": ["uuid-1", "uuid-2"]
        },
        {
          "adset_name": "Set 2 - Medium performers",
          "daily_budget_cents": 1700,
          "creatives": ["uuid-3", "uuid-4"]
        },
        {
          "adset_name": "Set 3 - New creatives",
          "daily_budget_cents": 1600,
          "creatives": ["uuid-5", "uuid-6"]
        }
      ],
      "reasoning": "Бюджет $50 → 3 adsets. ВЕСЬ бюджет распределен: $17+$17+$16=$50",
      "status": "success"
    }
  ]
}
```

**Проверки:**
- ✅ 1700 + 1700 + 1600 = 5000 (весь бюджет использован)
- ✅ Все 6 креативов распределены
- ✅ Ad sets созданы в статусе ACTIVE

### Сценарий 2: Направление с бюджетом $30 и 9 креативами

**Ожидаемый результат:**
- 3 ad sets по $10 каждый (1000 + 1000 + 1000 = 3000)
- Все 9 креативов распределены по 3 в каждом adset
- Ad sets включены

### Сценарий 3: Явное отключение auto_activate

**Команда:**
```bash
curl -X POST http://localhost:8082/api/campaign-builder/auto-launch-v2 \
  -H "Content-Type: application/json" \
  -d '{
    "user_account_id": "uuid-пользователя",
    "auto_activate": false
  }'
```

**Ожидаемый результат:**
- Ad sets созданы в статусе PAUSED (как запрошено)
- Бюджет распределен полностью

---

## 📝 Ключевые изменения (Summary)

1. **Промпт LLM** (`campaignBuilder.ts`):
   - ✅ Добавлено критическое напоминание о полном распределении бюджета в начало промпта
   - ✅ Обновлена логика формирования adsets с акцентом на использование ВСЕГО бюджета
   - ✅ Добавлена формула расчета: `budget_per_adset = ОКРУГЛИТЬ(total_budget / number_of_adsets)`
   - ✅ Обновлены все примеры с конкретными расчетами ($17 + $17 + $16 = $50)
   - ✅ Добавлен новый Пример Б для бюджета $50 с 3 adsets
   - ✅ Обновлены правила с критическим правилом #4
   - ✅ Изменен `auto_activate` на `true` по умолчанию

2. **Код маршрута** (`campaignBuilder.ts` route):
   - ✅ Изменена логика `auto_activate`: теперь `true` по умолчанию
   - ✅ Явно `false` только если передано в запросе

---

## 🎯 Результат

После внесения изменений:

1. **LLM всегда распределяет весь бюджет направления** между всеми создаваемыми ad sets
2. **Ad sets создаются включенными (ACTIVE)** по умолчанию
3. **Пользователь может явно отключить auto-activate**, передав `auto_activate: false` в запросе
4. **Логи содержат reasoning** с явным указанием как распределен бюджет

---

**Изменения протестированы:** ✅ Линтер не выявил ошибок  
**Обратная совместимость:** ✅ Сохранена (можно явно передать `auto_activate: false`)  
**Готово к продакшену:** ✅ Да

