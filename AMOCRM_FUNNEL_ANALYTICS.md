# AmoCRM - Аналитика воронки продаж

## Обзор

Интеграция с AmoCRM для отслеживания лидов по этапам воронки продаж. Система автоматически сопоставляет лиды из рекламных кампаний с контактами в AmoCRM по номеру телефона и отображает распределение по этапам воронки для каждого креатива.

## Функциональность

### 1. Автоматическая синхронизация
- **Частота:** Каждый час (настраивается через `AMOCRM_LEADS_SYNC_CRON_SCHEDULE`)
- **Процесс:**
  1. Получение всех лидов из локальной БД с телефонами
  2. Получение всех контактов из AmoCRM с их телефонами
  3. Сопоставление по нормализованным телефонам
  4. Обновление `current_status_id`, `current_pipeline_id`, `is_qualified` в таблице `leads`

### 2. Ручной запуск синхронизации
- **Endpoint:** `POST /api/amocrm/sync-leads?userAccountId={uuid}`
- **Доступ:** Кнопка "Обновить данные из AmoCRM" в UI
- **Использование:** Для немедленного получения актуальных данных

### 3. Статистика по креативам
- **Endpoint:** `GET /api/amocrm/creative-funnel-stats`
- **Параметры:**
  - `userAccountId` (обязательный)
  - `creativeId` (обязательный)
  - `directionId` (опционально)
  - `dateFrom` (опционально)
  - `dateTo` (опционально)
- **Возвращает:**
  ```json
  {
    "total_leads": 10,
    "stages": [
      {
        "stage_name": "Новый лид",
        "pipeline_name": "Воронка",
        "color": "#99ccff",
        "sort_order": 30,
        "count": 5,
        "percentage": 50
      },
      {
        "stage_name": "ПРОШЕЛ КОНСУЛЬТАЦИЮ",
        "pipeline_name": "Воронка",
        "color": "#99ccff",
        "sort_order": 60,
        "count": 5,
        "percentage": 50
      }
    ]
  }
  ```

## UI Интеграция

### ROI Analytics
В разделе **ROI Analytics** для каждого креатива доступна кнопка с иконкой воронки (🔍).

**При клике открывается модальное окно с:**
- Общим количеством лидов
- Таблицей распределения по этапам:
  - Название этапа
  - Название воронки
  - Количество лидов
  - Процент от общего числа
  - Прогресс-бар с цветом этапа
- Кнопкой "Обновить данные из AmoCRM"

## Технические детали

### Нормализация телефонов
```typescript
function normalizePhone(phone: string | null): string | null {
  if (!phone) return null;
  
  // Удаляем суффиксы WhatsApp
  let cleaned = phone.replace(/@s\.whatsapp\.net|@c\.us/g, '');
  
  // Удаляем все не-цифры
  cleaned = cleaned.replace(/\D/g, '');
  
  // Заменяем начальную 8 на 7 для российских номеров
  if (cleaned.startsWith('8') && cleaned.length === 11) {
    cleaned = '7' + cleaned.substring(1);
  }
  
  return cleaned || null;
}
```

### Структура БД

**Таблица `leads`:**
- `amocrm_lead_id` - ID лида в AmoCRM (может быть null)
- `current_status_id` - Текущий статус лида в воронке
- `current_pipeline_id` - ID воронки
- `is_qualified` - Квалифицированный ли лид
- `creative_id` - ID креатива из рекламы
- `direction_id` - ID направления рекламы
- `phone` - Телефон контакта
- `chat_id` - ID чата (часто содержит телефон)

**Таблица `amocrm_pipeline_stages`:**
- `status_id` - ID статуса в AmoCRM
- `status_name` - Название статуса
- `status_color` - Цвет для UI
- `pipeline_id` - ID воронки
- `pipeline_name` - Название воронки
- `sort_order` - Порядок сортировки
- `is_qualified_stage` - Квалифицирующий этап

## API Reference

### POST /api/amocrm/sync-leads
Запускает синхронизацию лидов с AmoCRM.

**Query параметры:**
- `userAccountId` (string, required) - UUID аккаунта пользователя

**Ответ:**
```json
{
  "success": true,
  "total": 107,
  "updated": 6,
  "errors": 0,
  "errorDetails": []
}
```

### GET /api/amocrm/creative-funnel-stats
Получает статистику распределения лидов по этапам воронки для указанного креатива.

**Query параметры:**
- `userAccountId` (string, required) - UUID аккаунта пользователя
- `creativeId` (string, required) - UUID креатива
- `directionId` (string, optional) - UUID направления (для фильтрации)
- `dateFrom` (string, optional) - Начальная дата в ISO формате
- `dateTo` (string, optional) - Конечная дата в ISO формате

**Ответ:** См. пример выше в разделе "Статистика по креативам"

## Cron Job

Автоматическая синхронизация настроена в сервисе `agent-brain`.

**Файл:** `services/agent-brain/src/amocrmLeadsSyncCron.js`

**Переменные окружения:**
- `AMOCRM_LEADS_SYNC_CRON_ENABLED` (default: `true`) - Включить/выключить cron
- `AMOCRM_LEADS_SYNC_CRON_SCHEDULE` (default: `0 * * * *`) - Cron расписание (каждый час)

**Логика:**
1. Каждый час получает список всех `user_accounts` с AmoCRM интеграцией
2. Для каждого аккаунта запускает `syncLeadsFromAmoCRM()`
3. Логирует результаты

## Производительность

### Оптимизация запросов
- **AmoCRM API:**
  - Лиды: пагинация по 250 записей
  - Контакты: пагинация по 250 записей
  - Всего ~20 запросов для 5000 контактов
  - Время выполнения: ~30 секунд

- **База данных:**
  - Индексы на `user_account_id`, `phone`, `chat_id`, `amocrm_lead_id`
  - Batch update для множественных лидов
  - Проверка изменений перед обновлением (только измененные поля)

### Ограничения AmoCRM API
- **Rate limit:** ~7 запросов в секунду
- **Batch requests:** AmoCRM не поддерживает получение контактов по списку ID через query string
- **Решение:** Получаем все контакты пагинацией и фильтруем локально

## Диагностика

### Проверка статуса синхронизации
```bash
# Ручной запуск
curl -X POST "https://app.performanteaiagency.com/api/amocrm/sync-leads?userAccountId=YOUR_UUID"

# Проверка результата
curl "https://app.performanteaiagency.com/api/leads?userAccountId=YOUR_UUID" | jq '{
  total: .leads | length,
  with_status: [.leads[] | select(.current_status_id != null)] | length,
  with_creative_and_status: [.leads[] | select(.creative_id != null and .current_status_id != null)] | length
}'
```

### Логи
```bash
# Agent-service (API)
docker-compose logs --tail=100 agent-service | grep -i amocrm

# Agent-brain (Cron)
docker-compose logs --tail=100 agent-brain | grep -i "amocrm.*sync"
```

## Развертывание

### 1. Применить миграции
```sql
-- Уже должны быть применены:
-- migrations/028_amocrm_pipeline_stages.sql
-- migrations/030_add_amocrm_client_credentials.sql
```

### 2. Собрать сервисы
```bash
cd ~/agents-monorepo

# Backend API
docker-compose build agent-service
docker-compose up -d agent-service

# Cron job
docker-compose build agent-brain
docker-compose up -d agent-brain

# Frontend (если изменялся)
docker-compose build frontend
docker-compose up -d frontend
```

### 3. Первая синхронизация
```bash
curl -X POST "https://app.performanteaiagency.com/api/amocrm/sync-leads?userAccountId=YOUR_UUID"
```

### 4. Проверка
- Откройте ROI Analytics в UI
- Найдите креатив с лидами
- Нажмите кнопку с иконкой воронки
- Проверьте отображение статистики

## Известные ограничения

1. **Сопоставление только по телефону**
   - Если у лида в БД нет телефона - он не будет сопоставлен
   - Если в AmoCRM у контакта другой телефон - сопоставление не произойдет

2. **Лиды без creative_id**
   - Отображаются в общей статистике
   - Исключены из статистики по креативам

3. **Задержка обновления**
   - Cron: до 1 часа
   - Ручное обновление: ~30 секунд

## Поддержка

При возникновении проблем проверьте:
1. Валидность AmoCRM токена (`/api/amocrm/status`)
2. Наличие телефонов у лидов в БД
3. Синхронизацию воронок (`/api/amocrm/sync-pipelines`)
4. Логи сервисов (см. раздел Диагностика)

