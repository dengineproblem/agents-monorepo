# Исправление проблемы с отображением статуса AmoCRM

## Проблема

Пользователь был подключен к AmoCRM (токены в БД), но в UI показывалась кнопка "Подключить" вместо "Подключено".

## Причина

В коде Profile.tsx использовался **неправильный ключ** для получения user ID:

```typescript
// ❌ НЕПРАВИЛЬНО:
user.userAccountId

// ✅ ПРАВИЛЬНО:
user.id
```

В localStorage объект `user` имеет поле `id`, а не `userAccountId`.

## Исправления

### 1. Profile.tsx - useEffect для загрузки статуса

**Было:**
```typescript
if (!user?.userAccountId) return;
const response = await fetch(`/api/amocrm/status?userAccountId=${user.userAccountId}`);
```

**Стало:**
```typescript
if (!user?.id) return;
const response = await fetch(`/api/amocrm/status?userAccountId=${user.id}`);
```

### 2. Все обработчики AmoCRM

Исправлено в:
- `handleAmoCRMConnectSubmit()` - подключение
- `handleAmoCRMDisconnect()` - отключение
- `handleAmoCRMSync()` - ручная синхронизация
- `loadAmoCRMStatus()` - загрузка статуса
- `loadWebhookStatus()` - проверка вебхука

### 3. Добавлены console.log для отладки

Теперь в консоли браузера будут видны:
```javascript
console.log('AmoCRM status loaded:', data);
console.log('Webhook status:', webhookData);
```

## Как проверить что проблема решена

### Вариант 1: В консоли браузера

1. Открыть DevTools (F12)
2. Открыть Profile страницу
3. В Console должно появиться:
   ```
   AmoCRM status loaded: { connected: true, subdomain: "ваш-поддомен" }
   Webhook status: { registered: true, ... }
   ```

### Вариант 2: Через скрипт (на сервере)

```bash
cd ~/agents-monorepo
./check-amocrm-connection.sh YOUR_USER_ID
```

Скрипт покажет:
- ✅/❌ Статус подключения в БД
- ✅/❌ Наличие токенов
- 📊 Ответ API

### Вариант 3: Проверить в БД напрямую

```sql
SELECT 
  id,
  username,
  amocrm_subdomain,
  CASE 
    WHEN amocrm_access_token IS NOT NULL THEN 'Есть токен'
    ELSE 'Нет токена'
  END as token_status,
  amocrm_token_expires_at
FROM user_accounts
WHERE id = 'YOUR_USER_ID';
```

### Вариант 4: Через API

```bash
curl "http://localhost:8082/amocrm/status?userAccountId=YOUR_USER_ID" | jq
```

Ожидаемый ответ:
```json
{
  "connected": true,
  "subdomain": "ваш-поддомен",
  "tokenExpiresAt": "2025-11-09T12:00:00.000Z"
}
```

## Deployment

После исправления нужно:

```bash
cd ~/agents-monorepo

# Пересобрать frontend
docker-compose build frontend
docker-compose up -d frontend

# Проверить что контейнер запустился
docker-compose ps frontend

# Очистить кеш в браузере (Ctrl+Shift+R)
```

## Тестирование после деплоя

1. **Открыть Profile в приватном окне** (чтобы не было кеша)
2. **Проверить что показывается правильный статус:**
   - Если подключено → "Подключено" + Badge "Webhook"
   - Если не подключено → "Подключить"
3. **Проверить в консоли браузера** что загружаются данные
4. **Попробовать подключение** (если еще не подключено)
5. **Проверить ручную синхронизацию** (кнопка в модальном окне)

## Известные ситуации

### Если user = null
Если пользователь не авторизован, `user` будет `null`. В этом случае:
- useEffect не выполнится (`if (!user?.id) return`)
- Кнопки будут disabled
- Это нормальное поведение

### Если в БД нет токенов
API `/amocrm/status` вернет:
```json
{
  "connected": false
}
```
И в UI правильно покажется "Подключить".

### Если токен истек
API автоматически обновит токен через refresh_token при следующем запросе к AmoCRM.

## Чек-лист проверки

- [x] Исправлен useEffect для загрузки статуса
- [x] Исправлены все обработчики (connect, disconnect, sync)
- [x] Добавлены console.log для отладки
- [x] Создан скрипт проверки подключения
- [x] Обновлена документация
- [ ] Протестировано на локальном окружении
- [ ] Задеплоено на production
- [ ] Протестировано на production

## Дополнительно

Если проблема повторится:

1. **Проверить структуру user в localStorage:**
   ```javascript
   console.log('User object:', localStorage.getItem('user'));
   console.log('Parsed:', JSON.parse(localStorage.getItem('user')));
   ```

2. **Проверить что поле id существует:**
   ```javascript
   const user = JSON.parse(localStorage.getItem('user'));
   console.log('User ID:', user?.id);
   console.log('Has userAccountId?', user?.userAccountId);
   ```

3. **Проверить network запросы в DevTools:**
   - Tab: Network
   - Filter: `/api/amocrm/status`
   - Проверить Query String Parameters

4. **Проверить логи backend:**
   ```bash
   docker-compose logs --tail=50 agent-service | grep "amocrm/status"
   ```



