# Fake Reports Service - Telegram Bot для генерации отчётов

Сервис для генерации поддельных отчётов агента Brain через Telegram бота. Используется для демонстрации и тестирования.

## 🎯 Назначение

Бот генерирует реалистичные отчёты агента Brain с:
- Фейковыми метриками (spend, leads, CPL близко к плановому ±20%)
- 2-3 вымышленными кампаниями с названиями от LLM
- AI-сгенерированными текстами действий, аналитики и рекомендаций
- Всеми вспомогательными показателями (quality leads 60-80%, ID кампаний)

## 🚀 Быстрый старт

### Локальная разработка

```bash
cd services/fake-reports-service

# Установка зависимостей
npm install

# Создание .env файла
cat > .env << EOF
TELEGRAM_BOT_TOKEN=your_bot_token_here
OPENAI_API_KEY=your_openai_key_here
OPENAI_MODEL=gpt-4o-mini
PORT=8086
NODE_ENV=development
EOF

# Запуск в dev режиме
npm run dev
```

### Production деплой

```bash
# На production сервере
cd /root/agents-monorepo

# Pull изменений
git pull origin main

# Создать .env (если ещё не создан)
cat > services/fake-reports-service/.env << EOF
TELEGRAM_BOT_TOKEN=8357970247:AAFuvmpSZCP0dj4rxu20IAzOxkJffOEhykc
OPENAI_API_KEY=your_key_here
OPENAI_MODEL=gpt-4o-mini
PORT=8086
NODE_ENV=production
EOF

# Build и запуск
docker-compose build fake-reports-service
docker-compose up -d fake-reports-service

# Настроить webhook
curl -X POST http://localhost:8086/telegram/setup-webhook \
  -H "Content-Type: application/json" \
  -d '{"webhook_url":"https://performanteaiagency.com/fake-reports/telegram/webhook"}'

# Проверить статус
docker ps | grep fake-reports
docker logs agents-monorepo-fake-reports-service-1 --tail 50
```

## 📝 Использование бота

### Формат команды

**Вариант 1 (рекомендуемый):**
```
/generate Стоматология 2.50
```

**Вариант 2 (с кавычками для ниш из нескольких слов):**
```
/generate "Юридические услуги" 3.00
```

**Вариант 3 (естественный язык):**
```
ниша: Фитнес, плановый CPL: 1.80
```

### Параметры

- **Ниша** - любая текстовая строка (например: "Стоматология", "Онлайн-курсы", "Недвижимость")
- **Плановый CPL** - желаемая стоимость заявки в USD (например: 2.50, 1.80, 5.00)

### Пример ответа бота

```
📊 ОТЧЕТ ПО РЕКЛАМНЫМ КАМПАНИЯМ
📅 Дата: 30.01.2026

✅ ОБЩАЯ СВОДКА
💰 Потрачено: $245.67
📨 Получено заявок: 98
💵 Средняя CPL: $2.51

---

📱 КАМПАНИЯ: [Стоматологи 30+] WhatsApp
🆔 ID: 120233428971230449
📊 Статус: ACTIVE
💰 Потрачено: $124.32
📨 Заявок: 51
💵 CPL: $2.44

✅ Качественные лиды: 39 (76%)
💎 Средняя стоимость качественной заявки: $3.19

...
```

## 🏗️ Архитектура

### Структура проекта

```
services/fake-reports-service/
├── src/
│   ├── config.ts                 # Конфигурация из .env
│   ├── server.ts                 # Fastify сервер
│   ├── types/
│   │   ├── report.ts            # Типы отчётов
│   │   └── metrics.ts           # Типы метрик
│   ├── lib/
│   │   ├── metricsGenerator.ts  # Генерация фейковых метрик
│   │   ├── llmService.ts        # Интеграция с OpenAI
│   │   ├── reportGenerator.ts   # Оркестрация генерации
│   │   ├── telegramHandler.ts   # Обработка Telegram сообщений
│   │   └── telegramApi.ts       # Telegram Bot API
│   ├── routes/
│   │   └── telegramWebhook.ts   # Webhook endpoints
│   └── utils/
│       └── formatters.ts         # Форматирование отчётов
├── Dockerfile
├── package.json
└── .env (не в git!)
```

### Технологии

- **Runtime:** Node.js 20
- **Framework:** Fastify 4
- **Language:** TypeScript
- **LLM:** OpenAI GPT-4o-mini
- **Bot API:** Telegram Bot API (raw, без библиотек)
- **Deployment:** Docker

### Поток данных

```
Telegram → Webhook → telegramHandler → reportGenerator
                                            ↓
                                    ┌───────┴────────┐
                                    ↓                ↓
                            llmService      metricsGenerator
                         (генерация текстов) (генерация метрик)
                                    ↓                ↓
                                    └───────┬────────┘
                                            ↓
                                     formatters
                                            ↓
                                    Telegram sendMessage
```

## 🔧 API Endpoints

### POST /telegram/webhook
Принимает обновления от Telegram Bot API.

**Payload:** стандартный Telegram Update object

### GET /health
Health check endpoint.

**Response:**
```json
{
  "status": "ok",
  "service": "fake-reports-service",
  "timestamp": "2026-01-30T12:30:36.453Z",
  "openai": "configured"
}
```

### POST /telegram/setup-webhook
Настраивает webhook для бота.

**Request:**
```json
{
  "webhook_url": "https://performanteaiagency.com/fake-reports/telegram/webhook"
}
```

**Response:**
```json
{
  "success": true,
  "webhook_url": "https://performanteaiagency.com/fake-reports/telegram/webhook",
  "message": "Webhook configured successfully"
}
```

### GET /telegram/webhook-info
Получить информацию о webhook.

**Response:**
```json
{
  "success": true,
  "webhook_info": {
    "url": "https://performanteaiagency.com/fake-reports/telegram/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    "max_connections": 40,
    "ip_address": "147.182.186.15",
    "allowed_updates": ["message"]
  }
}
```

## 📊 Алгоритм генерации метрик

### Бюджет
- Общий spend: случайное значение от $150 до $500
- Распределение по кампаниям: взвешенное случайное (основная кампания получает больше)

### CPL
- Базовый CPL: плановый ± 20% variance
- Случайное отклонение для каждой кампании

### Качество лидов
- Процент качественных: 60-80% (случайно)
- Quality CPL: базовый CPL × (1 + случайный множитель 0.2-0.5)

### ID генерация
- Campaign ID: 18-значное число (как у Facebook)
- Ad Account ID: формат `act_XXXXXXXXXX`
- User ID: UUID v4

## 🤖 LLM промпты

### Генерация названий кампаний
- **Model:** gpt-4o-mini
- **Temperature:** 0.7
- **Задача:** Создать 2-3 реалистичных названия для Facebook кампаний по нише

### Генерация текста действий
- **Model:** gpt-4o-mini
- **Temperature:** 0.3 (точность)
- **Задача:** Описать выполненные действия (оптимизация, изменение таргетинга)

### Генерация аналитики
- **Model:** gpt-4o-mini
- **Temperature:** 0.7
- **Задача:** Анализ трендов и динамики кампаний

### Генерация рекомендаций
- **Model:** gpt-4o-mini
- **Temperature:** 0.7
- **Задача:** Предложить действия для улучшения показателей

## 🔒 Безопасность

- `.env` файл в `.gitignore` (не коммитится)
- API ключи хранятся только на сервере
- Webhook использует HTTPS
- Нет аутентификации в боте (учитывать при публичном использовании)

## 🐛 Troubleshooting

### Бот не отвечает

**Проверить статус контейнера:**
```bash
docker ps | grep fake-reports
```

**Проверить логи:**
```bash
docker logs agents-monorepo-fake-reports-service-1 --tail 50
```

**Проверить webhook:**
```bash
curl http://localhost:8086/telegram/webhook-info
```

### Webhook не принимает запросы

**Проверить nginx:**
```bash
docker logs agents-monorepo-nginx-1 --tail 50
```

**Проверить location в nginx-production.conf:**
```nginx
location /fake-reports/ {
    proxy_pass http://fake-reports-service:8086/;
    ...
}
```

### OpenAI ошибки

**Проверить API ключ:**
```bash
docker exec agents-monorepo-fake-reports-service-1 env | grep OPENAI
```

**Проверить квоту OpenAI:**
Зайти в https://platform.openai.com/usage

## 📝 TODO / Улучшения

- [ ] Добавить аутентификацию пользователей
- [ ] Добавить Redis для кэширования LLM ответов
- [ ] Добавить rate limiting
- [ ] Добавить поддержку разных языков
- [ ] Добавить выбор периода отчёта (день/неделя/месяц)
- [ ] Добавить графики через Chart.js
- [ ] Добавить экспорт в PDF

## 📄 Лицензия

Внутренний проект. Не для публичного использования.

---

**Автор:** Performante AI Agency
**Дата создания:** 30.01.2026
**Версия:** 1.0.0
