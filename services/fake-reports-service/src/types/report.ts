/**
 * Типы для структуры отчетов
 */

export interface GenerateReportRequest {
  niche: string;           // Ниша (например: "Стоматология", "Фитнес")
  targetCpl: number;       // Плановая стоимость заявки в USD
}

export interface Campaign {
  id: string;              // Campaign ID (18-значное число)
  name: string;            // Название кампании (например: "[Мамы] WhatsApp")
  status: 'Активна' | 'Неактивна';
  spend: number;           // Затраты в USD
  leads: number;           // Количество лидов
  cpl: number;             // Стоимость за лид (CPL)
  qualityLeads: number;    // Количество качественных лидов
  qualityCpl: number;      // Стоимость за качественный лид
  qualityPercent: number;  // Процент качественных лидов (0-100)
}

export interface FakeReport {
  // Метаданные
  date: string;                    // Дата отчета (YYYY-MM-DD)
  userId: string;                  // UUID пользователя
  username: string;                // Username для отображения
  adAccountId: string;             // ID рекламного аккаунта (act_XXXXXXXXXX)
  accountStatus: 'Активен' | 'Неактивен';

  // Общая сводка
  totalSpend: number;              // Общие затраты (USD)
  totalLeads: number;              // Общее количество лидов
  totalCpl: number;                // Общий CPL (USD)
  totalQualityLeads: number;       // Общее количество качественных лидов
  totalQualityCpl: number;         // Общий CPL качественного лида (USD)

  // Кампании
  campaigns: Campaign[];

  // LLM-генерированные тексты
  actionsText: string;             // Раздел "✅ Выполненные действия"
  analyticsText: string;           // Раздел "📊 Аналитика в динамике"
  recommendationsText: string;     // Раздел "Для дальнейшей оптимизации"
}
