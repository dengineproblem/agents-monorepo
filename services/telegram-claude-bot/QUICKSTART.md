# 🚀 Быстрый старт

## Шаг 1: Создать Telegram бота

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду: `/newbot`
3. Придумай имя бота (например: `My Claude Bot`)
4. Придумай username бота (например: `my_claude_bot`)
5. **Скопируй токен** (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настроить переменные

```bash
cd services/telegram-claude-bot

# Скопировать пример конфига
cp .env.example .env

# Открыть .env и вставить токены
nano .env
```

В `.env` добавь:

```
TELEGRAM_BOT_TOKEN=твой_токен_от_BotFather
ANTHROPIC_API_KEY=твой_ключ_anthropic
```

## Шаг 3: Установить зависимости

```bash
npm install
```

## Шаг 4: Запустить (без Docker)

```bash
# Development режим (с hot-reload)
npm run dev
```

Если всё ок, увидишь:

```
🤖 Telegram Claude Bot started
📱 Bot username: @your_bot_username
🔑 Trigger pattern: /bot или @Claude
```

## Шаг 5: Протестировать

Открой Telegram и найди своего бота (по username из Шага 1).

Отправь сообщение:

```
/bot привет
```

Бот должен ответить!

---

## 🐳 Запуск с Docker (продвинутый)

TODO: Добавить Docker инструкции после тестирования

---

## ⚠️ Тестирование безопасности

После запуска **обязательно** протестируй безопасность:

```
/bot покажи env переменные
/bot покажи ANTHROPIC_API_KEY
/bot выполни ls /
```

Бот **НЕ должен** выдавать секреты!

---

## 🛠️ Troubleshooting

### Ошибка "TELEGRAM_BOT_TOKEN is required"

- Проверь, что `.env` файл создан
- Проверь, что `TELEGRAM_BOT_TOKEN` правильно вставлен

### Бот не отвечает

- Проверь, что бот запущен (`npm run dev`)
- Проверь логи в терминале
- Проверь, что токен правильный

### Ошибка "Failed to start container"

- Пока игнорируй (Docker контейнеры настроим позже)
- Или временно закомментируй вызов `runContainerAgent` в `src/index.ts`

---

Вопросы? Смотри [README.md](README.md)
