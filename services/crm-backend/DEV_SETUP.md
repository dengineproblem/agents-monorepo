# 🛠️ CRM Backend - Настройка локальной разработки

## Проблема

WhatsApp instance подключен на **production сервере**, поэтому:
- 🔴 Локальная Evolution PostgreSQL БД пустая или содержит старые данные
- 🔴 Реальные WhatsApp сообщения находятся на production сервере
- ✅ Для анализа диалогов нужен доступ к production БД

## Решение: SSH туннель

Создаём безопасный SSH туннель к production Evolution PostgreSQL для доступа к реальным данным.

---

## 🚀 Быстрый старт

### Автоматический способ (рекомендуется)

```bash
# Из корня проекта
./scripts/start-crm-dev.sh
```

Скрипт автоматически:
1. ✅ Проверяет и создаёт SSH туннель к production БД
2. ✅ Запускает Docker контейнеры (crm-backend, chatbot-service)
3. ✅ Запускает Vite dev server для crm-frontend
4. ✅ Проверяет подключение к БД
5. ✅ Открывает браузер на http://localhost:5174

### Остановка

```bash
./scripts/stop-crm-dev.sh
```

---

## 📋 Требования

1. **SSH доступ к production серверу**
   ```bash
   ssh root@147.182.186.15
   # Должен работать без пароля (через SSH ключ)
   ```

2. **Переменные окружения** (`.env.crm`)
   ```bash
   # Файл уже должен быть настроен с параметрами:
   EVOLUTION_DB_HOST=host.docker.internal
   EVOLUTION_DB_PORT=5434
   EVOLUTION_DB_NAME=evolution
   EVOLUTION_DB_USER=evolution
   EVOLUTION_DB_PASSWORD=evolution_secure_password_2024
   ```

3. **Docker и Docker Compose**
   ```bash
   docker --version  # >= 20.x
   docker-compose --version  # >= 1.29.x
   ```

---

## 🔧 Ручная настройка SSH туннеля

Если хочешь настроить вручную без скрипта:

### Шаг 1: Запустить SSH туннель

```bash
# Создать туннель (остаётся в фоне)
ssh -L 5434:localhost:5433 root@147.182.186.15 -N -f

# Проверить что туннель работает
lsof -i:5434
# Должен показать ssh процесс
```

**Объяснение параметров:**
- `-L 5434:localhost:5433` — проброс порта (локальный 5434 → удалённый 5433)
- `-N` — не выполнять команды (только туннель)
- `-f` — работа в фоновом режиме

### Шаг 2: Запустить crm-backend

```bash
# Из корня проекта
docker-compose up -d crm-backend

# Проверить логи
docker logs -f agents-monorepo-crm-backend-1
# Должно быть: "Connected to Evolution PostgreSQL"
```

### Шаг 3: Запустить crm-frontend

```bash
cd services/crm-frontend
npm run dev
# Откроется http://localhost:5174
```

### Шаг 4: Остановить SSH туннель

```bash
# Найти процесс
lsof -ti:5434

# Остановить (замени PID на реальный)
kill <PID>
```

---

## 🔍 Проверка подключения

### Проверка SSH туннеля

```bash
# Проверить что порт 5434 слушается
lsof -i:5434

# Попробовать подключиться через туннель (из хоста)
psql -h localhost -p 5434 -U evolution -d evolution
# Пароль: evolution_secure_password_2024
```

### Проверка crm-backend

```bash
# Логи backend
docker logs agents-monorepo-crm-backend-1 --tail 50

# Должно быть:
# ✅ "Connected to Evolution PostgreSQL" с host: host.docker.internal
# ❌ Если "Evolution PostgreSQL pool error" — проблема с туннелем
```

### Проверка данных в БД

```bash
# Войти в контейнер crm-backend
docker exec -it agents-monorepo-crm-backend-1 sh

# Установить psql (если нужно)
apk add postgresql-client

# Подключиться к БД
psql -h host.docker.internal -p 5434 -U evolution -d evolution

# Проверить данные
SELECT COUNT(*) FROM "Instance";
SELECT COUNT(*) FROM "Message";
```

---

## 🐛 Troubleshooting

### Проблема: "SSH туннель не запускается"

**Симптом:**
```
Error: Не удалось подключиться к root@147.182.186.15
```

**Решения:**

1. Проверить SSH ключи:
   ```bash
   ssh root@147.182.186.15 echo "OK"
   # Должно вернуть "OK" без запроса пароля
   ```

2. Проверить доступность сервера:
   ```bash
   ping 147.182.186.15
   ```

3. Проверить существующий туннель:
   ```bash
   lsof -i:5434
   # Если порт занят — закрыть старый туннель
   kill $(lsof -ti:5434)
   ```

---

### Проблема: "crm-backend не может подключиться к БД"

**Симптом:**
```
Evolution PostgreSQL pool error: connect ECONNREFUSED
```

**Решения:**

1. Проверить что SSH туннель работает:
   ```bash
   lsof -i:5434
   # Должен быть ssh процесс
   ```

2. Проверить `.env.crm`:
   ```bash
   cat .env.crm | grep EVOLUTION_DB_HOST
   # Должно быть: host.docker.internal
   ```

3. Проверить `docker-compose.yml`:
   ```bash
   cat docker-compose.yml | grep -A 10 "crm-backend:"
   # Должен быть extra_hosts с host.docker.internal
   ```

4. Перезапустить crm-backend:
   ```bash
   docker-compose restart crm-backend
   docker logs -f agents-monorepo-crm-backend-1
   ```

---

### Проблема: "Порт 5434 уже занят"

**Симптом:**
```
bind: Address already in use
```

**Решение:**
```bash
# Найти процесс на порту 5434
lsof -i:5434

# Остановить процесс
kill $(lsof -ti:5434)

# Или force kill
kill -9 $(lsof -ti:5434)

# Запустить туннель заново
ssh -L 5434:localhost:5433 root@147.182.186.15 -N -f
```

---

### Проблема: "SSH туннель упал во время работы"

**Симптом:**
Backend логи показывают:
```
Evolution PostgreSQL pool error: Connection terminated unexpectedly
```

**Решение:**
```bash
# Проверить туннель
lsof -i:5434

# Если нет — перезапустить
ssh -L 5434:localhost:5433 root@147.182.186.15 -N -f

# Перезапустить backend
docker-compose restart crm-backend
```

---

### Проблема: "Vite не запускается"

**Симптом:**
```
Error: Port 5174 already in use
```

**Решение:**
```bash
# Найти процесс на порту 5174
lsof -i:5174

# Остановить
kill $(lsof -ti:5174)

# Или остановить через скрипт
./scripts/stop-crm-dev.sh

# Запустить заново
cd services/crm-frontend
npm run dev
```

---

### Проблема: "Таймаут подключения к БД"

**Симптом:**
```
Connection timeout
```

**Решения:**

1. Проверить файрвол на production:
   ```bash
   ssh root@147.182.186.15
   sudo ufw status
   # Порт 5433 должен быть доступен локально
   ```

2. Увеличить таймауты в `evolutionDb.ts` (если нужно):
   ```typescript
   connectionTimeoutMillis: 30000, // было 10000
   ```

---

## 🔄 Переключение между локальной и production БД

### Использовать production БД (текущая настройка)

```bash
# .env.crm
EVOLUTION_DB_HOST=host.docker.internal
EVOLUTION_DB_PORT=5434  # SSH туннель
```

### Использовать локальную БД

```bash
# .env.crm
EVOLUTION_DB_HOST=evolution-postgres
EVOLUTION_DB_PORT=5432  # Локальный контейнер
```

После изменения:
```bash
docker-compose restart crm-backend
```

---

## 📊 Архитектура

```
Локальная машина                    Production сервер
┌─────────────────────┐            ┌──────────────────────┐
│                     │            │                      │
│  crm-frontend       │            │                      │
│  (Vite :5174)       │            │                      │
│         ↓           │            │                      │
│  crm-backend        │            │                      │
│  (Docker :8084) ────┼──────SSH───┼→ evolution-postgres  │
│         ↓           │   tunnel   │   (:5433)            │
│  host.docker        │   :5434    │                      │
│  .internal:5434     │            │                      │
│         ↓           │            │   WhatsApp messages  │
│  SSH tunnel ────────┼────────────┼→ (production data)   │
│  localhost:5434     │            │                      │
└─────────────────────┘            └──────────────────────┘
```

**Важно:**
- 🔵 Чтение данных: из production Evolution PostgreSQL
- 🟢 Запись результатов: в Supabase (общая БД для dev и prod)
- 🔒 Production БД используется **только для чтения** (анализ сообщений)

---

## 📝 Полезные команды

```bash
# Проверить все порты
lsof -i:5434  # SSH туннель
lsof -i:5174  # Vite frontend
lsof -i:8084  # CRM backend
lsof -i:8083  # Chatbot service

# Статус Docker контейнеров
docker ps | grep crm

# Логи всех сервисов
docker-compose logs -f crm-backend chatbot-service

# Перезагрузить всё
./scripts/stop-crm-dev.sh
./scripts/start-crm-dev.sh
```

---

## 📚 Дополнительная документация

- **INFRASTRUCTURE.md** — полная инфраструктура проекта
- **LOCAL_VS_PRODUCTION_ANALYSIS.md** — различия локальной и production среды
- **FRONTEND_API_CONVENTIONS.md** — правила работы с API
- **services/crm-backend/README.md** — API endpoints и система скоринга

---

## ✅ Чеклист перед началом разработки

- [ ] SSH ключи настроены для `root@147.182.186.15`
- [ ] Файл `.env.crm` создан с правильными параметрами
- [ ] Docker и Docker Compose установлены и работают
- [ ] Скрипт `./scripts/start-crm-dev.sh` успешно запускается
- [ ] Backend подключается к production БД (проверить логи)
- [ ] Frontend открывается на http://localhost:5174
- [ ] Анализ диалогов работает (тестовый запрос)

---

Готово! Теперь можешь разрабатывать CRM с доступом к реальным production данным 🚀



