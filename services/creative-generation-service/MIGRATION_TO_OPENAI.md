# Миграция с Gemini на OpenAI

## ✅ Выполненные изменения

### 1. Замена зависимостей
- ❌ Удалено: `@google/generative-ai` 
- ✅ Добавлено: `openai` ^4.65.0

### 2. Обновлены файлы

#### `src/services/openai.ts` (новый файл, заменяет gemini.ts)
- Использует OpenAI SDK для генерации текстов через **GPT-4o-mini**
- Генерация фоновых изображений через **DALL-E 3** (размер 1024x1792)
- ⚠️ **ВАЖНО**: DALL-E 3 НЕ накладывает текст на изображения (только фон)

#### `src/server.ts`
- Заменен импорт с `initializeGeminiAPI` на `initializeOpenAI`
- Обновлена проверка переменной окружения: `OPENAI_API_KEY` вместо `GEMINI_API_KEY`
- Обновлено приветственное сообщение при запуске

#### `src/routes/texts.ts`
- Заменен импорт с `generateText from gemini` на `generateText from openai`

#### `src/routes/image.ts`
- Заменен импорт с `generateCreativeImage from gemini` на `generateCreativeImage from openai`
- Добавлено скачивание изображения с временного URL OpenAI
- Обновлены сообщения об ошибках

#### `src/db/supabase.ts`
- Добавлена поддержка множественных вариантов имени переменной Supabase:
  - `SUPABASE_SERVICE_KEY`
  - `SUPABASE_SERVICE_ROLE`
  - `SUPABASE_SERVICE_ROLE_KEY`
- Добавлено логирование при инициализации

### 3. Переменные окружения

Обновлены переменные окружения в `.env`:

```bash
# Старые (Gemini)
GEMINI_API_KEY=...

# Новые (OpenAI)
OPENAI_API_KEY=...
OPENAI_MODEL=gpt-4o-mini
```

### 4. Документация
- Обновлен `README.md` с информацией об OpenAI
- Создан `SETUP_ENV.md` с инструкциями по настройке
- Создан скрипт `setup-env.sh` для автоматической настройки

---

## 🚀 Запуск после миграции

### Быстрый старт

1. Настройте переменные окружения:
```bash
cd services/creative-generation-service
./setup-env.sh
```

2. Установите зависимости:
```bash
npm install
```

3. Соберите проект:
```bash
npm run build
```

4. Запустите сервис:
```bash
npm run dev
```

5. Проверьте работоспособность:
```bash
curl http://localhost:8085/health
```

### Тестирование API

#### Генерация текста (offer)
```bash
curl -X POST http://localhost:8085/generate-offer \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "ваш-user-id",
    "prompt": "Информация о бизнесе и целевой аудитории"
  }'
```

#### Генерация буллетов
```bash
curl -X POST http://localhost:8085/generate-bullets \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "ваш-user-id",
    "prompt": "Информация о преимуществах продукта"
  }'
```

---

## ⚠️ Важные отличия от Gemini

### Генерация изображений

**Gemini 3 Pro Image Preview:**
- ✅ Генерирует изображение с готовым текстом
- ✅ Размер 1080x1920 (точно под Instagram Stories)
- ✅ Все элементы на одном изображении

**OpenAI DALL-E 3:**
- ⚠️ Генерирует ТОЛЬКО фоновое изображение
- ⚠️ Размер 1024x1792 (близко к 9:16)
- ❌ НЕ накладывает текст на изображение
- 💡 Требуется дополнительная обработка для наложения текста

### Рекомендации

Для полноценной работы с текстом на изображениях рассмотрите:

1. **Библиотека Sharp** - для наложения текста на Node.js
```bash
npm install sharp
```

2. **Canvas API** - для композитинга изображений

3. **Вернуться к Gemini** - если критично получать текст на изображении за один запрос

4. **Использовать Midjourney API** - альтернативный вариант

---

## 📊 Сравнение моделей

| Параметр | Gemini 1.5 Flash | GPT-4o-mini |
|----------|------------------|-------------|
| Скорость | Быстрая | Очень быстрая |
| Стоимость | $0.075/1M токенов | $0.150/1M токенов (input) |
| Качество текста | Отлично | Отлично |
| Контекст | 1M токенов | 128K токенов |

| Параметр | Gemini 3 Pro Image | DALL-E 3 |
|----------|-------------------|----------|
| Текст на изображении | ✅ Да | ❌ Нет |
| Размер | 1080x1920 | 1024x1792 |
| Качество | HD | HD |
| Стоимость | ~$0.05/изображение | $0.080/изображение (HD) |

---

## 🔧 Troubleshooting

### Проблема: "User not found"
**Решение:** Проблема с подключением к Supabase. Убедитесь:
- `SUPABASE_URL` установлен правильно
- `SUPABASE_SERVICE_KEY` содержит Service Role Key (не anon key)
- Пользователь существует в таблице `user_accounts`

### Проблема: "OPENAI_API_KEY is required"
**Решение:** Запустите `./setup-env.sh` или вручную добавьте ключ в `.env`

### Проблема: Изображения без текста
**Ожидаемое поведение:** DALL-E 3 генерирует только фон. Для добавления текста нужна дополнительная обработка.

---

## 📝 TODO (для полной функциональности)

- [ ] Добавить библиотеку для наложения текста на изображения (Sharp или Canvas)
- [ ] Реализовать масштабирование изображения с 1024x1792 до 1080x1920
- [ ] Добавить кэширование сгенерированных фонов
- [ ] Оптимизировать промпты для DALL-E 3

---

## ✅ Результат миграции

- ✅ Сервис успешно запускается
- ✅ Все эндпоинты доступны
- ✅ Генерация текстов работает через GPT-4o-mini
- ✅ Supabase подключение настроено
- ⚠️ Генерация изображений требует доработки для текста

Дата миграции: 21 ноября 2025

