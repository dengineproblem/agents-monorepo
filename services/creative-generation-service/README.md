# Creative Generation Service

Микросервис для генерации рекламных креативов через Google Gemini API.

## Возможности

- ✅ Генерация текстов для креативов (offer, bullets, profits, cta) через Gemini
- ✅ Генерация креативов с текстом через **Gemini 3 Pro Image Preview**
- ✅ **НОВОЕ:** Поддержка референсных изображений для сохранения стиля
- ✅ Формат 1080x1920 (Instagram Stories)
- ✅ Автоматическое наложение текста на изображение
- ✅ Загрузка готовых изображений в Supabase Storage
- ✅ Управление лимитами генераций

## Установка

```bash
cd services/creative-generation-service
npm install
```

## Настройка

1. Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

2. Получите API ключ Google AI на https://aistudio.google.com/app/apikey

3. Заполните переменные окружения в `.env`:

```bash
# Google Gemini API Key
GEMINI_API_KEY=your-gemini-api-key-here

# Supabase Configuration (используйте один из вариантов)
SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_KEY=your-supabase-service-role-key
# Альтернативные имена (поддерживаются):
# SUPABASE_SERVICE_ROLE=your-key
# SUPABASE_SERVICE_ROLE_KEY=your-key

# Server Configuration
PORT=8085
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info
CORS_ORIGIN=*
```

## Запуск

### Development

```bash
npm run dev
```

Сервис будет доступен на `http://localhost:8085`

### Production

```bash
npm run build
npm start
```

## API Endpoints

### Генерация текстов

- `POST /generate-offer` - генерация заголовка
- `POST /generate-bullets` - генерация буллетов
- `POST /generate-profits` - генерация выгоды
- `POST /generate-cta` - генерация CTA

### Генерация изображения

- `POST /generate-creative` - генерация финального креатива с текстом
  - Поддерживает референсные изображения (URL или base64)
  - См. [QUICK_REFERENCE_IMAGE.md](./QUICK_REFERENCE_IMAGE.md) для примеров

## Архитектура

```
src/
├── routes/          # API эндпоинты
│   ├── texts.ts     # Генерация текстов
│   └── image.ts     # Генерация изображений
├── services/        # Бизнес-логика
│   ├── gemini.ts    # Клиент Gemini API
│   └── prompts.ts   # System prompts
├── db/              # База данных
│   └── supabase.ts  # Supabase клиент
├── types/           # TypeScript типы
│   └── index.ts
└── server.ts        # Главный файл сервиса
```

## Зависимости

- **@google/generative-ai**: Google Gemini API SDK
- **fastify**: ^4.28.0 - Веб-фреймворк
- **@supabase/supabase-js**: ^2.45.0 - Supabase клиент

## Важные замечания

### Генерация изображений с текстом

✅ **Gemini 3 Pro Image Preview** поддерживает автоматическое наложение текста на изображения! Вам не нужны дополнительные инструменты.

### Референсные изображения

Вы можете передавать референсные изображения для сохранения фирменного стиля:
- 🎨 Брендинг - логотип или существующие креативы
- 🖼️ Стилистика - примеры дизайна из Pinterest/Behance
- 📐 Композиция - желаемое расположение элементов

📖 **Подробная документация**: [REFERENCE_IMAGE_GUIDE.md](./REFERENCE_IMAGE_GUIDE.md)  
🚀 **Быстрый старт**: [QUICK_REFERENCE_IMAGE.md](./QUICK_REFERENCE_IMAGE.md)

## Лицензия

ISC

