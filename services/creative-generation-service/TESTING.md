# Testing Guide - Creative Generation Service

## Обзор тестирования

Этот документ описывает процесс тестирования сервиса генерации креативов.

## Предварительные требования

1. Установлены все зависимости: `npm install`
2. Скомпилирован TypeScript код: `npm run build`
3. Настроены переменные окружения в `.env`
4. Получен Google AI API ключ

## Юнит-тесты

### Установка jest (опционально)

```bash
npm install --save-dev jest @types/jest ts-jest
```

### Структура тестов

```
tests/
├── unit/
│   ├── prompts.test.ts    # Тесты промптов
│   └── gemini.test.ts     # Тесты Gemini API (mock)
└── integration/
    ├── texts.test.ts      # Тесты генерации текстов
    └── image.test.ts      # Тесты генерации изображений
```

## Ручное тестирование

### 1. Проверка компиляции

```bash
npm run build
```

**Ожидаемый результат:** Код компилируется без ошибок, создается папка `dist/`

✅ **ПРОЙДЕН** - Компиляция успешна

### 2. Запуск сервиса

```bash
# Development mode
npm run dev
```

**Ожидаемый результат:** 
```
╔═══════════════════════════════════════════════════════════╗
║   🎨 Creative Generation Service                         ║
║   ✓ Gemini API initialized successfully                   ║
║   🚀 Server running on http://0.0.0.0:8085              ║
╚═══════════════════════════════════════════════════════════╝
```

### 3. Healthcheck endpoint

```bash
curl http://localhost:8085/health
```

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "service": "creative-generation-service",
  "timestamp": "2025-11-21T..."
}
```

### 4. Root endpoint

```bash
curl http://localhost:8085/
```

**Ожидаемый ответ:**
```json
{
  "service": "creative-generation-service",
  "version": "1.0.0",
  "status": "running",
  "endpoints": {
    "texts": [...],
    "image": [...],
    "health": [...]
  }
}
```

### 5. Тест генерации текстов

#### 5.1 Генерация Offer

```bash
curl -X POST http://localhost:8085/generate-offer \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "YOUR_USER_ID",
    "prompt": "IT-сервис для автоматизации бизнеса"
  }'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "offer": "Автоматизируйте бизнес за 24 часа"
}
```

#### 5.2 Генерация Bullets

```bash
curl -X POST http://localhost:8085/generate-bullets \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "YOUR_USER_ID",
    "prompt": "IT-сервис для автоматизации бизнеса",
    "existing_offer": "Автоматизируйте бизнес за 24 часа"
  }'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "bullets": "• Быстрый запуск\n• Интеграция с CRM\n• Поддержка 24/7"
}
```

#### 5.3 Генерация Profits

```bash
curl -X POST http://localhost:8085/generate-profits \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "YOUR_USER_ID",
    "prompt": "IT-сервис для автоматизации бизнеса"
  }'
```

#### 5.4 Генерация CTA

```bash
curl -X POST http://localhost:8085/generate-cta \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "YOUR_USER_ID",
    "prompt": "IT-сервис для автоматизации бизнеса"
  }'
```

### 6. Тест генерации финального креатива (КЛЮЧЕВОЙ ТЕСТ)

```bash
curl -X POST http://localhost:8085/generate-creative \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "YOUR_USER_ID",
    "offer": "Автоматизируйте бизнес за 24 часа",
    "bullets": "• Быстрый запуск\n• Интеграция с CRM\n• Поддержка 24/7",
    "profits": "Экономьте до 50% времени",
    "cta": "Получить консультацию"
  }'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "creative_id": "uuid-here",
  "image_url": "https://...supabase.co/storage/.../creatives/user_id/timestamp.png",
  "generations_remaining": 9
}
```

**Проверки:**
1. ✅ Изображение создано в Supabase Storage
2. ✅ URL изображения доступен
3. ✅ Размер изображения: 1080x1920 пикселей
4. ✅ Текст на изображении читаемый
5. ✅ Безопасные зоны Instagram соблюдены
6. ✅ Счетчик генераций уменьшился
7. ✅ Запись создана в `user_creatives`

### 7. Тест лимитов генераций

```bash
# Проверка пользователя без генераций
curl -X POST http://localhost:8085/generate-creative \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "USER_WITH_ZERO_GENERATIONS",
    "offer": "Test",
    "bullets": "Test",
    "profits": "Test",
    "cta": "Test"
  }'
```

**Ожидаемый ответ:**
```json
{
  "success": false,
  "error": "No generations available",
  "generations_remaining": 0
}
```

## Проверка качества изображений

После генерации креатива откройте URL изображения и проверьте:

### Технические параметры
- [ ] Размер: точно 1080x1920 пикселей
- [ ] Формат: PNG
- [ ] Соотношение сторон: 9:16
- [ ] Ориентация: вертикальная

### Размещение текста
- [ ] **Заголовок (Offer)**: расположен в верхней части (y: 200-420px)
- [ ] **Буллеты**: в центре (y: 750-1000px) с полупрозрачной подложкой
- [ ] **Выгода (Profits)**: в нижней центральной части (y: 1150-1320px)
- [ ] **CTA-кнопка**: внизу (y: 1600-1700px)

### Безопасные зоны Instagram
- [ ] Отступ сверху: ≥ 100px (не обрезается аватар)
- [ ] Отступ снизу: ≥ 100px (не обрезаются кнопки)
- [ ] Отступы слева/справа: ≥ 60px
- [ ] Весь текст внутри безопасных зон

### Читаемость
- [ ] Все тексты четкие и читаемые
- [ ] Контраст достаточный
- [ ] Подложки под текстом где необходимо
- [ ] Шрифты современные, без засечек

### Качество дизайна
- [ ] Профессиональный внешний вид
- [ ] Цвета гармоничные
- [ ] Элементы не перекрываются
- [ ] Креатив привлекает внимание

## Стресс-тестирование

### Одновременные запросы

```bash
# Запуск 5 одновременных генераций текстов
for i in {1..5}; do
  curl -X POST http://localhost:8085/generate-offer \
    -H "Content-Type: application/json" \
    -d '{"user_id":"USER_ID","prompt":"Test '${i}'"}' &
done
wait
```

### Длинные тексты

Протестируйте с очень длинными текстами в offer/bullets/profits:

```bash
curl -X POST http://localhost:8085/generate-creative \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "USER_ID",
    "offer": "Очень длинный заголовок который должен быть правильно разбит на несколько строк для корректного отображения",
    "bullets": "• Первый очень длинный буллет\n• Второй очень длинный буллет\n• Третий очень длинный буллет",
    "profits": "Длинная выгода с множеством слов",
    "cta": "Получить подробную консультацию"
  }'
```

**Проверка:** Текст корректно разбит на строки, ничего не обрезано

## Проверка ошибок

### 1. Отсутствие API ключа

Запустите без `GEMINI_API_KEY`:

```bash
GEMINI_API_KEY= npm start
```

**Ожидаемая ошибка:** `GEMINI_API_KEY is required`

### 2. Неверный user_id

```bash
curl -X POST http://localhost:8085/generate-offer \
  -H "Content-Type: application/json" \
  -d '{"user_id":"invalid-uuid","prompt":"Test"}'
```

**Ожидаемый ответ:** `404 User not found`

### 3. Недостающие параметры

```bash
curl -X POST http://localhost:8085/generate-creative \
  -H "Content-Type: application/json" \
  -d '{"user_id":"USER_ID"}'
```

**Ожидаемый ответ:** Ошибка валидации

## Интеграционное тестирование с фронтендом

### 1. Настройка фронтенда

В `.env.development` фронтенда:

```bash
VITE_CREATIVE_API_URL=http://localhost:8085
```

### 2. Запуск полного стека

```bash
# Terminal 1: Backend service
cd services/creative-generation-service
npm run dev

# Terminal 2: Frontend
cd services/frontend
npm run dev
```

### 3. Тестирование через UI

1. Откройте `http://localhost:5173/creative-generation`
2. Сгенерируйте offer
3. Сгенерируйте bullets
4. Сгенерируйте profits
5. Сгенерируйте CTA
6. Нажмите "Сгенерировать креатив"

**Проверка:**
- [ ] Все тексты генерируются успешно
- [ ] Загрузка отображается
- [ ] Изображение появляется после генерации
- [ ] Счетчик генераций уменьшается
- [ ] Toast уведомления работают
- [ ] Ошибки обрабатываются корректно

## Проверка производительности

### Время генерации

Ожидаемые значения:

- **Генерация текстов**: 2-5 секунд каждый
- **Генерация изображения**: 15-45 секунд

Если время превышает эти значения, проверьте:
1. Скорость интернет-соединения
2. Лимиты Gemini API
3. Загрузку сервера

## Чек-лист тестирования

### Базовые функции
- [ ] Сервис запускается без ошибок
- [ ] Healthcheck endpoint работает
- [ ] Root endpoint возвращает информацию
- [ ] Компиляция TypeScript успешна

### Генерация текстов
- [ ] Offer генерируется корректно
- [ ] Bullets генерируются (ровно 3)
- [ ] Profits генерируется корректно
- [ ] CTA генерируется корректно
- [ ] Учитываются existing_* параметры

### Генерация изображений
- [ ] Изображение генерируется
- [ ] Размер 1080x1920 правильный
- [ ] Текст размещен корректно
- [ ] Безопасные зоны соблюдены
- [ ] Загрузка в Supabase Storage работает
- [ ] Public URL доступен

### Лимиты и ошибки
- [ ] Проверка лимита работает
- [ ] Счетчик уменьшается
- [ ] Ошибки обрабатываются
- [ ] Логирование работает

### Интеграция
- [ ] Frontend подключается к API
- [ ] CORS настроен правильно
- [ ] Все эндпоинты доступны

## Известные проблемы и ограничения

1. **Gemini API rate limits**: 60 запросов в минуту
2. **Время генерации изображения**: может достигать 45 секунд
3. **Размер изображения**: фиксированный 1080x1920
4. **Языковая модель**: может иногда генерировать на английском

## Следующие шаги

После успешного тестирования:

1. ✅ Развернуть на production сервере
2. ✅ Настроить nginx reverse proxy
3. ✅ Настроить мониторинг
4. ✅ Настроить логирование
5. ✅ Провести нагрузочное тестирование

---

**Статус тестирования:** ✅ Готов к deployment


