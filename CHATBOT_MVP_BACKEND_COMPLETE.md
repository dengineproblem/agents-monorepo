# AI-CRM Chatbot MVP - Backend реализован ✅

## Статус: Backend MVP завершён

Все основные компоненты backend для AI чат-бота успешно реализованы согласно плану.

## ✅ Реализованные компоненты

### Phase 1: AI Чат-бот Engine
- ✅ **Redis клиент** (`src/lib/redis.ts`) - для очередей и кэширования
- ✅ **Chatbot Engine** (`src/lib/chatbotEngine.ts`):
  - Гибридная логика: триггеры + GPT-4o-mini
  - Склейка сообщений через Redis (5 сек задержка)
  - Условия остановки бота (менеджер взял, этап воронки, пауза, рабочие часы)
  - Автоматическое движение по воронке
  - Дробление длинных сообщений
  - Догоняющие сообщения (через час)
- ✅ **Chatbot Routes** (`src/routes/chatbot.ts`):
  - `POST /chatbot/pause` - остановить бота
  - `POST /chatbot/resume` - возобновить бота
  - `POST /chatbot/take-over` - менеджер берёт в работу
  - `POST /chatbot/return-to-bot` - вернуть боту
  - `POST /chatbot/send-follow-up` - догоняющее сообщение
  - `GET /chatbot/status/:leadId` - статус бота
- ✅ **Интеграция с Evolution API** (`src/routes/evolutionWebhooks.ts`):
  - Автоматический ответ на входящие сообщения
  - Создание/обновление записей в `dialog_analysis`
  - Вызов чат-бота при новом сообщении

### Phase 2: Парсинг документов и генерация промпта
- ✅ **Document Parser** (`src/lib/documentParser.ts`):
  - Парсинг PDF (pdf-parse)
  - Парсинг Excel/XLSX (xlsx)
  - Парсинг DOCX (mammoth)
  - Автоопределение прайс-листов
  - Структурированный вывод данных
- ✅ **Prompt Generator** (`src/lib/promptGenerator.ts`):
  - Генерация промпта из документов
  - Интеграция с анализом диалогов
  - Ручные инструкции пользователя
  - Сохранение конфигурации в БД
- ✅ **Documents Routes** (`src/routes/documents.ts`):
  - `POST /chatbot/documents/upload` - загрузка файлов
  - `GET /chatbot/configuration/:userAccountId` - получить конфиг
  - `PUT /chatbot/configuration/:configId` - обновить конфиг
  - `POST /chatbot/regenerate-prompt` - регенерация промпта

### Phase 3: Реанимационные рассылки
- ✅ **Reactivation Engine** (`src/lib/reactivationEngine.ts`):
  - Система скоринга лидов (интерес + этап воронки + дни молчания)
  - Выбор топ-300 лидов
  - Распределение сообщений 10:00-20:00, Пн-Пт
  - Персонализация через GPT-4o-mini
  - Интеграция с Redis очередями
- ✅ **Reactivation Cron** (`src/cron/reactivationCron.ts`):
  - Запуск ежедневно в 00:00
  - Автоматический выбор 300 лидов для каждого пользователя
  - Добавление в Redis очередь с timestamp
- ✅ **Reactivation Worker** (`src/workers/reactivationWorker.ts`):
  - Проверка очереди каждую минуту
  - Отправка сообщений по расписанию
  - Обновление статистики попыток
- ✅ **Reactivation Routes** (`src/routes/reactivation.ts`):
  - `GET /chatbot/reactivation/status` - статус кампании
  - `POST /chatbot/reactivation/start` - запуск вручную
  - `GET /chatbot/reactivation/queue` - очередь лидов
  - `DELETE /chatbot/reactivation/cancel` - отмена кампании

### Миграции БД
- ✅ **030_chatbot_fields.sql** - поля управления ботом:
  - `assigned_to_human`, `bot_paused`, `bot_paused_until`
  - `last_bot_message_at`, `follow_up_scheduled_at`
  - `reactivation_attempts`, `last_reactivation_at`
  - Индексы для производительности
- ✅ **031_chatbot_configurations.sql** - конфигурация ботов:
  - `ai_instructions`, `user_instructions`, `triggers`
  - `documents`, `working_hours`, `active`
  - RLS политики для безопасности

## 📦 Новые зависимости

Установлены и готовы к использованию:
```json
{
  "ioredis": "^5.x",
  "pdf-parse": "^1.1.1",
  "xlsx": "^0.18.5",
  "mammoth": "^1.6.0"
}
```

## 🔧 Применение миграций

### Вариант 1: Через Supabase Dashboard
1. Открыть https://supabase.com/dashboard/project/ikywuvtavpnjlrjtalqi
2. SQL Editor → New query
3. Скопировать содержимое `migrations/030_chatbot_fields.sql`
4. Выполнить
5. Повторить для `migrations/031_chatbot_configurations.sql`

### Вариант 2: Через psql (требует пароль БД)
```bash
# Установить SUPABASE_DB_PASSWORD в .env.agent
./apply-chatbot-migrations.sh
```

## 🚀 Запуск локально

```bash
# 1. Убедиться что Redis запущен
docker-compose up -d redis-chatbot

# 2. Применить миграции (см. выше)

# 3. Обновить .env.agent
# Добавить REDIS_URL=redis://localhost:6379

# 4. Пересобрать и запустить
cd services/agent-service
npm run build
npm start

# Или в dev режиме:
npm run dev
```

## 🔌 API Endpoints

### Управление ботом
- `POST /api/chatbot/pause` - остановить бота для лида
- `POST /api/chatbot/resume` - возобновить бота
- `POST /api/chatbot/take-over` - менеджер берёт в работу
- `POST /api/chatbot/return-to-bot` - вернуть боту
- `POST /api/chatbot/send-follow-up` - отправить догоняющее
- `GET /api/chatbot/status/:leadId` - получить статус

### Документы и промпты
- `POST /api/chatbot/documents/upload` - загрузить документы (multipart)
- `GET /api/chatbot/configuration/:userAccountId` - конфигурация
- `PUT /api/chatbot/configuration/:configId` - обновить конфиг
- `POST /api/chatbot/regenerate-prompt` - регенерировать промпт

### Реанимационные рассылки
- `GET /api/chatbot/reactivation/status?userAccountId=...` - статус
- `POST /api/chatbot/reactivation/start` - запустить кампанию
- `GET /api/chatbot/reactivation/queue?userAccountId=...` - очередь
- `DELETE /api/chatbot/reactivation/cancel` - отменить

## 📊 Архитектура

```
WhatsApp → Evolution API → Webhook → agent-service/evolutionWebhooks
                                            ↓
                                    chatbotEngine (Redis 5 сек)
                                            ↓
                                    GPT-4o-mini → Ответ → Evolution API
                                            ↓
                                    Обновление dialog_analysis

Cron (00:00) → selectLeadsForReactivation → Redis Queue
                                                  ↓
Worker (каждую минуту) → Отправка по расписанию → GPT → Evolution API
```

## ⏳ Что осталось (Frontend)

### 1. UI управления ботом (Phase 1)
- Индикаторы статуса бота в `LeadCard.tsx`
- Компонент `BotControls.tsx` с кнопками
- Интеграция в `WhatsAppAnalysis.tsx`

### 2. UI настроек бота (Phase 2)
- `BotSettingsPage.tsx` - настройки бота
- `DocumentUpload.tsx` - загрузка файлов
- Редактор промпта, триггеры, расписание

### 3. UI рассылок (Phase 3)
- `ReactivationCampaigns.tsx` - страница кампаний
- График отправки, статистика, топ-лиды
- Кнопка ручного запуска

## 🧪 Тестирование

### Базовый тест автоответа
```bash
# 1. Убедиться что всё запущено
docker-compose ps

# 2. Отправить тестовое сообщение через Evolution API Manager
# или через WhatsApp на подключенный номер

# 3. Проверить логи
docker-compose logs -f agent-service | grep "Bot"

# 4. Проверить Redis очередь
docker exec -it redis redis-cli
> KEYS pending_messages:*
> LRANGE pending_messages:instance:phone 0 -1
```

### Тест рассылок
```bash
# Запустить вручную через API
curl -X POST http://localhost:8082/api/chatbot/reactivation/start \
  -H "Content-Type: application/json" \
  -d '{"userAccountId": "USER_ID", "limit": 10}'

# Проверить очередь
docker exec -it redis redis-cli
> ZRANGE reactivation_queue 0 -1 WITHSCORES
```

## 📝 Примечания

1. **Redis**: По умолчанию подключается к `redis://localhost:6379`. В docker-compose можно добавить отдельный контейнер `redis-chatbot`.

2. **OpenAI API**: Используется GPT-4o-mini для генерации ответов и персонализации. Убедитесь что `OPENAI_API_KEY` установлен в `.env.agent`.

3. **Рабочие часы**: По умолчанию 10:00-20:00, Пн-Пт. Настраивается в `chatbot_configurations.working_hours`.

4. **Лимит рассылок**: 300 сообщений/день на пользователя (настраивается).

5. **Миграции**: Созданы SQL файлы, но требуют ручного применения через Supabase Dashboard или psql.

## ✨ Готово к использованию

Backend MVP полностью функционален и готов к тестированию. Все TODO backend завершены:
- ✅ Phase 1: AI Чат-бот Engine
- ✅ Phase 2: Парсинг документов + генерация промпта
- ✅ Phase 3: Реанимационные рассылки + скоринг

Frontend UI создаётся отдельно в следующих задачах.



