# AmoCRM Integration - Полная интеграция завершена

## Что реализовано

### Backend (agent-service)

1. **Модуль управления вебхуками** (`src/lib/amocrmWebhook.ts`)
   - `registerAmoCRMWebhook()` - автоматическая регистрация вебхука
   - `getWebhookStatus()` - проверка статуса вебхука
   - `unregisterAmoCRMWebhook()` - отмена регистрации

2. **Улучшенная обработка вебхуков** (`src/workflows/amocrmSync.ts`)
   - Поиск лидов по `amocrm_lead_id`
   - Fallback поиск по телефону если лид не найден
   - Автоматическое связывание `amocrm_lead_id` при находке по телефону

3. **API endpoints** (`src/routes/amocrmManagement.ts`)
   - `GET /api/amocrm/webhook-status` - проверить статус вебхука
   - `POST /api/amocrm/register-webhook` - зарегистрировать вебхук вручную
   - `POST /api/amocrm/unregister-webhook` - отменить регистрацию вебхука

4. **Автоматическая регистрация** (`src/routes/amocrmOAuth.ts`)
   - Вебхук автоматически регистрируется после успешного OAuth

5. **Новая функция в адаптере** (`src/adapters/amocrm.ts`)
   - `getContact()` - получение контакта по ID

### Frontend

1. **ConnectionsGrid** (`src/components/profile/ConnectionsGrid.tsx`)
   - Добавлена поддержка AmoCRM
   - Badge для отображения статуса вебхука
   - Иконка AmoCRM

2. **Profile страница** (`src/pages/Profile.tsx`)
   - Состояния для AmoCRM
   - useEffect для загрузки статуса
   - Обработчики подключения/отключения/синхронизации
   - Модальное окно подключения (ввод поддомена)
   - Модальное окно управления (синхронизация, отключение, статус вебхука)

### Конфигурация

1. **Переменные окружения** (`env.agent.example`)
   - `APP_URL` - URL приложения для вебхуков
   - `AMOCRM_LEADS_SYNC_CRON_SCHEDULE` - расписание fallback синхронизации

2. **Cron job** (`services/agent-brain/src/amocrmLeadsSyncCron.js`)
   - Изменено расписание с каждого часа на каждые 6 часов
   - Служит fallback вариантом на случай проблем с вебхуками

## Как протестировать

### 1. Подключение AmoCRM

1. Открыть страницу Profile
2. Найти карточку "AmoCRM" в разделе подключений
3. Нажать кнопку "Подключить"
4. Ввести поддомен AmoCRM (например: `mycompany`)
5. Нажать "Подключить"
6. Будет редирект на AmoCRM для авторизации
7. После авторизации вебхук должен автоматически зарегистрироваться

**Проверить:**
- ✅ Статус "Подключено" в Profile
- ✅ Badge "Webhook" отображается (если вебхук активен)

### 2. Статус вебхука

1. Открыть карточку AmoCRM (нажать на неё если уже подключен)
2. Проверить индикатор статуса вебхука:
   - 🟢 Зеленый = вебхук активен
   - 🟡 Желтый = вебхук не настроен

**Проверить через API:**
```bash
curl "https://app.performanteaiagency.com/api/amocrm/webhook-status?userAccountId=YOUR_UUID"
```

### 3. Изменение статуса лида в AmoCRM

1. Создать тестовый лид в вашей системе (через WhatsApp или вручную)
2. В AmoCRM найти контакт с тем же телефоном
3. Изменить статус лида в AmoCRM (переместить по воронке)
4. Вебхук должен сработать автоматически (в течение нескольких секунд)
5. Проверить в БД что `current_status_id` и `current_pipeline_id` обновились

**Проверить в БД:**
```sql
SELECT id, phone, current_status_id, current_pipeline_id, is_qualified, updated_at
FROM leads
WHERE user_account_id = 'YOUR_UUID'
ORDER BY updated_at DESC
LIMIT 10;
```

**Проверить логи вебхука:**
```bash
docker-compose logs --tail=100 agent-service | grep "Processing lead status change"
```

### 4. Ручная синхронизация

1. Открыть модальное окно управления AmoCRM (нажать на карточку)
2. Нажать "Синхронизировать данные вручную"
3. Дождаться уведомления с результатом
4. Проверить что лиды обновились

**Проверить через API:**
```bash
curl -X POST "https://app.performanteaiagency.com/api/amocrm/sync-leads?userAccountId=YOUR_UUID"
```

### 5. Отключение AmoCRM

1. Открыть модальное окно управления AmoCRM
2. Нажать "Отключить AmoCRM"
3. Подтвердить действие
4. Проверить что статус изменился на "Не подключено"

**Проверить в БД:**
```sql
SELECT id, amocrm_subdomain, amocrm_access_token
FROM user_accounts
WHERE id = 'YOUR_UUID';
```

## Логи для диагностики

### Backend (agent-service)
```bash
# Все события AmoCRM
docker-compose logs --tail=200 agent-service | grep -i amocrm

# Вебхуки
docker-compose logs --tail=100 agent-service | grep "AmoCRM webhook"

# Регистрация вебхука
docker-compose logs --tail=50 agent-service | grep "webhook registered"
```

### Backend (agent-brain - cron)
```bash
# Fallback синхронизация
docker-compose logs --tail=100 agent-brain | grep "AmoCRM leads sync"
```

## Архитектура решения

### Реал-тайм синхронизация (основная)
```
AmoCRM → Webhook → /api/webhooks/amocrm → processLeadStatusChange → Update DB
```

**Преимущества:**
- ✅ Мгновенное обновление (1-2 секунды)
- ✅ Минимальная нагрузка на API AmoCRM
- ✅ Нет rate limits

### Fallback синхронизация (каждые 6 часов)
```
Cron (6h) → /api/amocrm/sync-leads → syncLeadsFromAmoCRM → Update DB
```

**Преимущества:**
- ✅ Резервный вариант на случай проблем с вебхуками
- ✅ Синхронизация для лидов без связанного `amocrm_lead_id`

### Поиск лидов по телефону
```
1. Ищем по amocrm_lead_id (быстро)
2. Если не найдено → получаем контакт из AmoCRM
3. Ищем по нормализованному телефону в phone или chat_id
4. Если найдено → связываем amocrm_lead_id для будущих обновлений
```

## Переменные окружения

Добавить в `.env.agent`:
```bash
# URL приложения для вебхуков
APP_URL=https://app.performanteaiagency.com

# Расписание fallback синхронизации (опционально)
AMOCRM_LEADS_SYNC_CRON_SCHEDULE=0 */6 * * *
```

## Известные ограничения

1. **Поиск только по телефону** - если у лида нет телефона, связывание невозможно
2. **Нормализация телефонов** - российские номера (8 → 7), WhatsApp суффиксы удаляются
3. **Вебхук на пользователя** - URL содержит `user_id`, нельзя использовать один вебхук для всех

## Deployment

1. **Обновить переменные окружения** на сервере
2. **Пересобрать сервисы:**
   ```bash
   docker-compose build agent-service agent-brain frontend
   docker-compose up -d
   ```
3. **Проверить логи:**
   ```bash
   docker-compose logs -f agent-service
   ```

## Готово! 🎉

Интеграция полностью реализована и готова к использованию.

**Следующие шаги:**
1. Протестировать на staging/production
2. Обновить документацию для пользователей
3. Мониторить логи вебхуков первую неделю







