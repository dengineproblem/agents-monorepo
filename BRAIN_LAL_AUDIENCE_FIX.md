# Brain Agent: Фикс проверки LAL аудитории

**Дата**: 2025-11-03  
**Статус**: ✅ Исправлено

## Проблема

Утром 3 ноября 2025 во время batch-запуска агента Brain произошла ошибка у двух пользователей:

- Агент Brain попытался создать дубликат ad set с локальной аудиторией (LAL - Lookalike Audience)
- У обоих пользователей `ig_seed_audience_id` не был заполнен в таблице `user_accounts`
- Ошибка возникла при выполнении действия `Audience.DuplicateAdSetWithAudience` с `audience_id: "use_lal_from_settings"`
- Агент не знал о том, что у пользователя нет настроенной LAL аудитории, и предложил действие которое не может быть выполнено

## Решение

Внесены изменения в `/services/agent-brain/src/server.js`:

### 1. Добавлен запрос `ig_seed_audience_id` из базы данных

**Файл**: `services/agent-brain/src/server.js` (строка 398)

```javascript
async function getUserAccount(userAccountId) {
  return await supabaseQuery('user_accounts', 
    async () => await supabase
      .from('user_accounts')
      .select('id, access_token, ad_account_id, page_id, telegram_id, telegram_id_2, telegram_id_3, telegram_id_4, telegram_bot_token, username, prompt3, plan_daily_budget_cents, default_cpl_target_cents, whatsapp_phone_number, ig_seed_audience_id')
      //                                                                                                                                                                                                                                                          ^^^^^^^^^^^^^^^^^^^^ ДОБАВЛЕНО
      .eq('id', userAccountId)
      .single(),
    { userAccountId }
  );
}
```

### 2. Передача информации о LAL аудитории в llmInput

**Файл**: `services/agent-brain/src/server.js` (строка 2313)

```javascript
const llmInput = {
  userAccountId,
  ad_account_id: ua?.ad_account_id || null,
  account: {
    timezone: ua?.account_timezone || 'Asia/Almaty',
    report_date: date,
    dispatch: !!inputs?.dispatch,
    report_only_mode: reportOnlyMode,
    has_lal_audience: !!ua?.ig_seed_audience_id  // ← ДОБАВЛЕНО
  },
  // ...
}
```

### 3. Обновлен системный промпт - секция LAL ДУБЛЬ

**Файл**: `services/agent-brain/src/server.js` (строки 1159-1170)

Добавлены критически важные проверки:

```
🎯 ПРИОРИТЕТ 3: LAL ДУБЛЬ (если нет креативов вообще)
- Применяется ТОЛЬКО если unused_creatives = [] И ready_creatives = []
- ⚠️ КРИТИЧЕСКИ ВАЖНО: ДОСТУПНО ТОЛЬКО если account.has_lal_audience === true!
- ⚠️ Если account.has_lal_audience === false - НЕ ИСПОЛЬЗУЙ это действие! Вместо этого упомяни в planNote что нужно настроить LAL аудиторию в Ads Manager и добавить её ID в настройки.
- Назначение: смена аудитории на LAL 3% IG Engagers 365d (когда нет креативов для ротации)
- Условия:
  • account.has_lal_audience === true (ОБЯЗАТЕЛЬНО! Проверяй ПЕРВЫМ делом)
  • HS ≤ -6 (slightly_bad или bad)
  • CPL_ratio ≥ 2.0 на yesterday ИЛИ last_3d
  • impr_yesterday ≥ 1000 ИЛИ impr_last_3d ≥ 1500
```

### 4. Обновлено описание действия в разделе ДОСТУПНЫЕ ДЕЙСТВИЯ

**Файл**: `services/agent-brain/src/server.js` (строка 1204)

```
- Audience.DuplicateAdSetWithAudience {"source_adset_id","audience_id","daily_budget?","name_suffix?"} — дубль ad set c заданной аудиторией (LAL3 IG Engagers 365d) без отключения Advantage+. ⚠️ ДОСТУПНО ТОЛЬКО если account.has_lal_audience === true! При audience_id="use_lal_from_settings" использует готовую LAL аудиторию из настроек пользователя.
```

## Что изменилось

### До фикса ❌

1. Агент Brain НЕ знал, есть ли у пользователя настроенная LAL аудитория
2. Агент мог предложить действие `Audience.DuplicateAdSetWithAudience` с `use_lal_from_settings` даже если аудитории нет
3. Ошибка возникала только при выполнении действия на уровне workflow
4. Пользователь получал ошибку в логах и Telegram

### После фикса ✅

1. Агент Brain **знает** о наличии/отсутствии LAL аудитории через поле `account.has_lal_audience`
2. Агент **не может** предложить действие `Audience.DuplicateAdSetWithAudience` если `has_lal_audience === false`
3. Если LAL аудитория не настроена, агент **упоминает в отчете** необходимость её настройки
4. Пользователь получает понятную рекомендацию вместо ошибки

## Как настроить LAL аудиторию для пользователя

Если у пользователя нет LAL аудитории:

1. Создать LAL аудиторию в Facebook Ads Manager:
   - Audience Type: Lookalike
   - Source: Instagram engagers за 365 дней
   - Location: Целевая страна/регион
   - Audience Size: 3% (оптимально для большинства случаев)

2. Скопировать ID созданной аудитории

3. Добавить ID в базу данных:

```sql
UPDATE user_accounts
SET ig_seed_audience_id = '123456789012345'  -- ID LAL аудитории из Ads Manager
WHERE id = 'user-uuid-here';
```

## Тестирование

После применения фикса рекомендуется:

1. Проверить batch-запуск агента Brain на тестовых пользователях
2. Убедиться что для пользователей БЕЗ `ig_seed_audience_id`:
   - Действие `Audience.DuplicateAdSetWithAudience` НЕ генерируется
   - В отчете есть упоминание о необходимости настройки LAL аудитории
3. Убедиться что для пользователей С `ig_seed_audience_id`:
   - Действие генерируется корректно при соблюдении условий
   - Workflow успешно выполняется

## Связанные файлы

- `/services/agent-brain/src/server.js` - основной файл агента Brain
- `/services/agent-service/src/workflows/campaignDuplicate.ts` - workflow для дублирования с аудиторией

## Приоритет действий агента Brain (напоминание)

При плохих результатах (slightly_bad / bad) агент использует следующий приоритет:

1. **ПРИОРИТЕТ 1**: Новый контент (`unused_creatives` > 0) → создать кампанию с новыми креативами
2. **ПРИОРИТЕТ 2**: Ротация креативов (`ready_creatives` > 0) → создать ad set с лучшими существующими креативами
3. **ПРИОРИТЕТ 3**: LAL дубль (только если `has_lal_audience === true` И нет креативов) → дублировать ad set с LAL аудиторией

Теперь ПРИОРИТЕТ 3 не сработает если не выполнено обязательное условие наличия LAL аудитории.

