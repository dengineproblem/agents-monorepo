# Функция ручной отправки кампаний

## 📋 Обзор

Реализована система ручной отправки очереди кампаний с умным планированием и управлением существующими очередями.

## ✨ Основные возможности

### 1. **Кнопка "Запустить отправку"**
- Расположена в компоненте `DailyCampaignQueue`
- Активна только когда есть сообщения в очереди
- Запускает умное планирование отправки

### 2. **Умное планирование**

#### Сценарий A: Рабочее время ⏰
```
Пример: 14:00, понедельник, рабочие часы 10-20
```
- ✅ Отправка начинается немедленно
- Сообщения распределяются до конца рабочего дня
- Показывает: скорость отправки и ожидаемую длительность

#### Сценарий B: Нерабочее время 🌙
```
Пример: 21:00, понедельник, рабочие часы 10-20
```
- ⏳ Отправка планируется на следующий рабочий день
- Показывает: когда начнётся отправка и параметры

#### Сценарий C: Выходной день 📅
```
Пример: Суббота, рабочие дни Пн-Пт
```
- ⏳ Отправка планируется на следующий понедельник

### 3. **Управление существующей очередью**

При нажатии "Сформировать очередь" система проверяет наличие существующих сообщений:

#### Автоматическое решение:
- **Очередь > 2 часов**: автоматически заменяется новой
- **Отправка уже началась**: новые лиды добавляются к существующим

#### Диалог выбора (очередь < 2 часов):
```
┌─────────────────────────────────────┐
│ ⚠️  Обнаружена существующая очередь │
│                                     │
│ В очереди: 150 сообщений           │
│ Создано: Вчера в 21:30             │
│                                     │
│ [Отмена] [Добавить] [Заменить]     │
└─────────────────────────────────────┘
```

**Заменить**: Удаляет старую очередь, создаёт новую  
**Добавить**: Добавляет новых лидов, пропускает дубликаты

## 🔧 Технические детали

### Новые файлы:

1. **`services/chatbot-service/src/lib/manualSendScheduler.ts`**
   - `checkExistingQueue()` - проверка существующей очереди
   - `determineSchedule()` - определение времени отправки
   - `decideQueueAction()` - решение о замене/слиянии
   - `markForManualSend()` - пометка сообщений для ручной отправки

2. **`services/crm-backend/migrations/009_add_manual_send_field.sql`**
   - Добавляет поле `manual_send_requested_at` в `campaign_messages`
   - Создаёт индексы для эффективных запросов

### Изменённые файлы:

1. **`services/chatbot-service/src/routes/campaign.ts`**
   - `POST /campaign/start-manual-send` - запуск ручной отправки
   - `GET /campaign/queue-status` - статус существующей очереди
   - Обновлён `POST /campaign/generate-queue` с поддержкой `action` параметра

2. **`services/chatbot-service/src/workers/campaignWorker.ts`**
   - Обрабатывает как autopilot, так и ручные отправки
   - Проверяет `manual_send_requested_at` для приоритета

3. **`services/crm-frontend/src/services/campaignApi.ts`**
   - `startManualSend()` - API для запуска отправки
   - `getQueueStatus()` - API для проверки статуса
   - `generateQueueWithAction()` - генерация с выбором действия

4. **`services/crm-frontend/src/components/campaigns/DailyCampaignQueue.tsx`**
   - Кнопка "Запустить отправку"
   - Диалог выбора действия с существующей очередью
   - Диалог информации о планировании отправки

## 📊 База данных

### Новое поле в `campaign_messages`:

```sql
manual_send_requested_at TIMESTAMPTZ NULL
```

Используется для:
- Пометки сообщений для ручной отправки
- Приоритета над автопилотом
- Отслеживания времени запроса

### Индексы:

```sql
CREATE INDEX idx_campaign_messages_manual_send 
ON campaign_messages(user_account_id, manual_send_requested_at) 
WHERE manual_send_requested_at IS NOT NULL;
```

## 🎯 Логика работы

### 1. Пользователь нажимает "Запустить отправку":

```typescript
1. Проверка наличия pending сообщений
2. Определение текущего времени и рабочих часов
3. Расчёт расписания:
   - Если рабочее время → immediate
   - Если нерабочее → scheduled на следующий рабочий день
4. Пометка сообщений: manual_send_requested_at = now()
5. Показ диалога с информацией о расписании
```

### 2. CampaignWorker (каждые 5 минут):

```typescript
1. Находит пользователей с:
   - autopilot_enabled = true ИЛИ
   - есть сообщения с manual_send_requested_at IS NOT NULL
2. Для каждого пользователя:
   - Проверяет рабочие часы
   - Рассчитывает slot limit
   - Отправляет batch сообщений
```

### 3. Генерация очереди:

```typescript
1. Проверка существующей очереди
2. Если очередь существует:
   - Возраст < 2 часов → показать диалог выбора
   - Возраст > 2 часов → автоматически заменить
   - Отправка началась → автоматически добавить
3. Генерация новой очереди
4. Фильтрация дубликатов при merge
5. Сохранение сообщений
```

## 🚀 Использование

### Для пользователя:

1. **Сформировать очередь**: 
   - Нажать "Сформировать очередь"
   - Если есть старая очередь → выбрать действие
   - Дождаться генерации

2. **Запустить отправку**:
   - Нажать "Запустить отправку"
   - Увидеть информацию о расписании
   - Отправка начнётся автоматически

3. **Очистить очередь**:
   - Нажать "Очистить очередь"
   - Все pending сообщения будут удалены

### Для разработчика:

```typescript
// Проверить статус очереди
const status = await campaignApi.getQueueStatus(userId);

// Запустить ручную отправку
const result = await campaignApi.startManualSend(userId);

// Сгенерировать очередь с действием
const queue = await campaignApi.generateQueueWithAction(userId, 'replace');
```

## ⚙️ Настройки

Настройки берутся из таблицы `campaign_settings`:

- `work_hours_start` - начало рабочего дня (по умолчанию 10)
- `work_hours_end` - конец рабочего дня (по умолчанию 20)
- `work_days` - рабочие дни [1,2,3,4,5] (Пн-Пт)
- `daily_message_limit` - лимит сообщений в день (по умолчанию 300)

## 📝 Примеры сценариев

### Сценарий 1: Вечерняя генерация
```
1. Пользователь в 21:00 формирует очередь (150 сообщений)
2. Нажимает "Запустить отправку"
3. Система показывает: "Отправка начнётся завтра в 10:00"
4. На следующий день в 10:00 начинается автоматическая отправка
```

### Сценарий 2: Обновление очереди
```
1. Утром сформирована очередь (100 сообщений)
2. Через час пользователь нажимает "Сформировать очередь" снова
3. Система показывает диалог: "Заменить или Добавить?"
4. Выбор "Добавить" → новые лиды добавляются, дубли пропускаются
```

### Сценарий 3: Немедленная отправка
```
1. В 14:00 (рабочее время) формируется очередь
2. Нажимается "Запустить отправку"
3. Система: "Отправка началась, ~25 сообщений/час, 6 часов"
4. Сообщения отправляются до конца рабочего дня
```

## 🔍 Отладка

### Проверить логи:
```bash
docker logs agents-monorepo-chatbot-service-1 --tail 100 | grep -E "(manual|queue)"
```

### Проверить pending сообщения:
```sql
SELECT count(*), manual_send_requested_at IS NOT NULL as is_manual
FROM campaign_messages 
WHERE status = 'pending' 
GROUP BY is_manual;
```

### Проверить работу worker:
```bash
docker logs agents-monorepo-chatbot-service-1 --tail 100 | grep "Campaign worker"
```

## ✅ Преимущества

- ✅ Уважает все лимиты (hourly rate limit)
- ✅ Умное планирование (не отправляет ночью/в выходные)
- ✅ Прозрачность (пользователь видит когда начнётся)
- ✅ Гибкость (можно запустить в любое время)
- ✅ Безопасность (автоматическая проверка рабочих часов)
- ✅ Управление очередями (замена/слияние)
- ✅ Приоритет ручной отправки над автопилотом

## 🎨 UI/UX

### Кнопки:
- 🔄 **Обновить** - обновить список очереди
- 🗑️ **Очистить очередь** - удалить все pending
- ✨ **Сформировать очередь** - создать новую очередь
- ▶️ **Запустить отправку** - начать отправку (новая!)

### Диалоги:
1. **Существующая очередь** - выбор действия
2. **Расписание отправки** - информация о планировании

### Цвета:
- Синий - основные действия (Запустить, Заменить)
- Серый - второстепенные (Обновить, Добавить)
- Красный - удаление (Очистить)

## 📚 Связанные файлы

- `CAMPAIGN_AUTOMATION_FLOW.md` - общая документация по кампаниям
- `CAMPAIGN_ANALYTICS.md` - аналитика кампаний
- `services/crm-backend/migrations/009_add_manual_send_field.sql` - миграция БД


